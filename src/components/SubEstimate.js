
import React, { useState, useEffect, useRef, useCallback } from 'react';
import { useNavigate } from 'react-router-dom';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { FileText } from 'lucide-react';

import { 
  faChevronRight, 
  faChevronDown, 
  faTrash, 
  faSpinner, 
  faPlus, 
  faSave, 
  faPencilAlt,
  faFileAlt,
  faInfoCircle,
  faSearch,
  faTimes ,
  faEdit,
  faRuler,
  faChevronUp,
  faCheck
  
} from '@fortawesome/free-solid-svg-icons';
import { motion, AnimatePresence } from 'framer-motion';
import toast, { Toaster } from 'react-hot-toast';
import MeasurementTable from './MeasurementTable';
import Stepper from '../components/Stepper';


const SubEstimateForm = () => {
  // State for work order information
  const [itemSearchQuery, setItemSearchQuery] = useState("");
  const [workOrderInfo, setWorkOrderInfo] = useState({
    workOrderId: '',
    nameOfWork: '',
    ssr: '',
    area: '',
    status: '',
    revisionStage: '',
    reviseId: '',
    reviseno: '',
    autogenerated: ''
  });
 


  // State for subwork management
  const [subworkName, setSubworkName] = useState('');
  const [subworkNameError, setSubworkNameError] = useState(false);
  const [subworks, setSubworks] = useState([]);
  const [isLoading, setIsLoading] = useState(false);
  const [expandedSubworks, setExpandedSubworks] = useState([]);
  const [searchQueries, setSearchQueries] = useState({}); 
  const [completedRate, setCompletedRate] = useState(0);
  const [currentSubworkForForm, setCurrentSubworkForForm] = useState(null);

  // State for item management
  const [items, setItems] = useState({});
  const [itemTotalQuantities, setItemTotalQuantities] = useState({});
  const [itemOptions, setItemOptions] = useState([]);
  const [showAddItemModal, setShowAddItemModal] = useState(false);
  const [currentSubworkId, setCurrentSubworkId] = useState(null);
  const [isItemLoading, setIsItemLoading] = useState(false);
 const [currentWorkOrderId, setCurrentWorkOrderId] = useState(null); 
 
  const [selectedItem, setSelectedItem] = useState(null);
  const [itemFormErrors, setItemFormErrors] = useState({});
  const [visibleMeasurements, setVisibleMeasurements] = useState({});
  // New state to track which row is in editing mode
  const [editingRowId, setEditingRowId] = useState(null);
  const [newItemRow, setNewItemRow] = useState(null);
  const [showItemDetailsModal, setShowItemDetailsModal] = useState(false);
  const [itemDetails, setItemDetails] = useState(null);
const [autoSaveTimeout, setAutoSaveTimeout] = useState(null);

  const [isItemDropdownOpen, setIsItemDropdownOpen] = useState(false);
// const [itemSearchQuery, setItemSearchQuery] = useState('');
  const [itemForm, setItemForm] = useState({
    id: 0,
    srNo: 0,
    itemNo: "",
    category: "",
    descriptionOfItem: "",
    floorLiftRise: "Ground floor",
    fkSubworkId: 0,
    fkWorkorderId: 0,
    completedRate: 0,
    labourRate: 0,
    scadaFlag: false,
    smallUnit: "",
    fullUnit: "",
    additionalSpecification: ""
  });
  const [editingItemId, setEditingItemId] = useState(null);

  // Animation variants
  const containerVariants = {
    hidden: { opacity: 0 },
    visible: { 
      opacity: 1,
      transition: { 
        duration: 0.5,
        when: "beforeChildren",
        staggerChildren: 0.1
      }
    }
  };

  const itemVariants = {
    hidden: { opacity: 0, y: 20 },
    visible: { opacity: 1, y: 0 }
  };

  // API base URL
  const API_BASE_URL = "https://24.101.103.87:8082/api";
  
  // JWT Token from localStorage
   const jwtToken = localStorage.getItem('authToken');

  const navigate = useNavigate();
  const subworkInputRef = useRef(null);

  // Load work order information from localStorage on component mount
   useEffect(() => {
    const workOrderId = localStorage.getItem('workorderId');
    const reviseId = localStorage.getItem('reviseId');
    
    // First load the basic information from localStorage
    const initialWorkOrderInfo = {
      workOrderId: workOrderId || '',
      nameOfWork: localStorage.getItem('nameOfWork') || '',
      ssr: localStorage.getItem('ssr') || '',
      area: localStorage.getItem('area') || '',
      status: localStorage.getItem('status') || '',
      revisionStage: localStorage.getItem('revisionStage') || '',
      reviseId: reviseId || '',
      reviseno: localStorage.getItem('reviseno') || '',
      autogenerated: localStorage.getItem('autogenerated') || ''
    };
      
      
       // Update state with localStorage values first
    setWorkOrderInfo(initialWorkOrderInfo);
    
    // // Load the data
    // const info = loadWorkOrderInfo();
   // Then fetch complete and up-to-date information from API
    const fetchWorkOrderDetails = async () => {
      if (!workOrderId) {
        toast.error('No work order ID found');
        return;
      }
      setIsLoading(true);
      try {
        const response = await fetch(`${API_BASE_URL}/workorders/${workOrderId}`, {
          headers: {
            "Authorization": `Bearer ${jwtToken}`,
            "Accept": "*/*"
          }
        });
      
        if (!response.ok) {
          throw new Error(`HTTP error! Status: ${response.status}`);
        }
        
        const data = await response.json();
        
        // Update state with fresh data from API
        setWorkOrderInfo(prevInfo => ({
          ...prevInfo,
          nameOfWork: data.nameOfWork || prevInfo.nameOfWork,
          ssr: data.ssr || prevInfo.ssr, 
          area: data.area || prevInfo.area,
          status: data.status || prevInfo.status,
          autogenerated: data.workOrderID || prevInfo.autogenerated
        }));
        
        // Also update localStorage with fresh data
        localStorage.setItem('nameOfWork', data.nameOfWork || '');
        localStorage.setItem('ssr', data.ssr || '');
        localStorage.setItem('area', data.area || '');
        localStorage.setItem('status', data.status || '');
        localStorage.setItem('autogenerated', data.workOrderID || '');
      } catch (error) {
        console.error("Error fetching work order details:", error);
        toast.error(`Failed to fetch work order details: ${error.message}`);
      }
    };
    
  
  // Also fetch current revision details
  const fetchCurrentRevision = async () => {
           console.log("Starting fetchCurrentRevision with reviseId:", reviseId, "workOrderId:", workOrderId);
      if (!reviseId) {
        console.log("No revision ID found, trying to fetch latest revision");
        
        // If no reviseId, try to get the latest revision for this work order
        if (workOrderId) {
          try {
            const response = await fetch(`${API_BASE_URL}/workorder-revisions/ByWorkorderId/${workOrderId}`, {
              headers: {
                "Authorization": `Bearer ${jwtToken}`,
                "Accept": "*/*"
              }
            });

   if (response.ok) {
              const revisions = await response.json();
              if (Array.isArray(revisions) && revisions.length > 0) {
                // Find the current revision (currentFlag = true) or the latest one
                const currentRevision = revisions.find(rev => rev.currentFlag) || revisions[revisions.length - 1];
            
                if (currentRevision) {
                       console.log("Selected current revision:", currentRevision);
                  setWorkOrderInfo(prevInfo => ({
                    ...prevInfo,
                      reviseId: currentRevision.id.toString(),
                    reviseno: currentRevision.reviseNumber || 'N/A',
                    revisionStage: currentRevision.revisionStage || ''
                  }));

                     localStorage.setItem('reviseId', currentRevision.id.toString());
                  localStorage.setItem('reviseno', currentRevision.reviseNumber || '');
                  localStorage.setItem('revisionStage', currentRevision.revisionStage || '');
                  
                  console.log(`Updated revision info: ${currentRevision.reviseNumber}`);
                    fetchSubworks(currentRevision.id.toString(), workOrderId);
                }
              }
            }
          } catch (error) {
            console.error("Error fetching revisions by work order ID:", error);
          }
        }
        return;
      }
      
       try {
        console.log(`Fetching revision details for reviseId: ${reviseId}`);
        const response = await fetch(`${API_BASE_URL}/workorder-revisions/${reviseId}`, {
          headers: {
            "Authorization": `Bearer ${jwtToken}`,
            "Accept": "*/*"
          }
        });

        if (!response.ok) {
          throw new Error(`HTTP error! Status: ${response.status}`);
        }

        const data = await response.json();  console.log("Fetched revision data:", data);

        // Check if data is an array (some APIs return single object in array) or single object
        const revisionData = Array.isArray(data) ? data[0] : data;
         if (revisionData) {
          const revisionNumber = revisionData.reviseNumber || 'N/A';
          const revisionStage = revisionData.revisionStage || '';
          
          console.log("Successfully extracted revision number:", revisionNumber);
        setWorkOrderInfo(prevInfo => {
            const updatedInfo = {
              ...prevInfo,
              reviseno: revisionNumber,
              revisionStage: revisionStage
            };
               console.log("Setting workOrderInfo to:", updatedInfo);
            return updatedInfo;
          });

        // Update localStorage with fresh revision data
  localStorage.setItem('reviseno', revisionNumber);
          localStorage.setItem('revisionStage', revisionStage);
          
          console.log("Updated localStorage with revision number:", revisionNumber);
        }
        // Fetch subworks after getting revision details
        fetchSubworks(reviseId, workOrderId);
        
      } catch (error) {
        console.error("Error fetching revision details:", error);
        toast.error(`Failed to fetch revision details: ${error.message}`);
      }
  
    };
    //     const data = await response.json();

    //     // Update state with fresh revision data
    //     setWorkOrderInfo(prevInfo => ({
    //       ...prevInfo,
    //       reviseno: data.reviseNumber || prevInfo.reviseno,
    //       revisionStage: data.revisionStage || prevInfo.revisionStage
    //     }));

    //     // Update localStorage with fresh revision data
    //     localStorage.setItem('reviseno', data.reviseNumber);
    //     localStorage.setItem('revisionStage', data.revisionStage);
    //   } catch (error) {
    //     console.error("Error fetching revision details:", error);
    //   }
    // };

    // Run both API calls if we have a work order ID
       if (workOrderId) {
      fetchWorkOrderDetails();
      // Add a small delay to prevent race conditions
      setTimeout(() => {
        fetchCurrentRevision();
      }, 100);
    }

    // Also fetch item options for later use
    fetchItemOptions();
  }, []);

  // Function to fetch revisions

  // Function to fetch revisions
  const fetchRevisions = async (workOrderId) => {
    if (!workOrderId) return;

    try {
      const response = await fetch(
        `${API_BASE_URL}/workorder-revisions/ByWorkorderId/${workOrderId}`,
        {
          headers: {
            Authorization: `Bearer ${jwtToken}`,
            Accept: '*/*',
          },
        }
      );

      if (!response.ok) throw new Error(`Error: ${response.status}`);

      const allRevisions = await response.json();

      const uniqueSorted = [...new Set(
        allRevisions
          .filter((rev) => rev.deletedFlag.toLowerCase() !== 'yes')
          .map((rev) => rev.reviseNumber)
      )].sort((a, b) => parseFloat(a) - parseFloat(b));

      console.log("Valid revisions:", uniqueSorted);
      toast.success(`Revision List: ${uniqueSorted.join(', ')}`);

    } catch (err) {
      console.error('Failed to fetch revisions:', err);
      toast.error('Error loading revision history.');
    }
  };
  

  // Format date for API calls
  const getFormattedDate = () => {
    const now = new Date();
    
    const year = now.getFullYear();
    const month = String(now.getMonth() + 1).padStart(2, '0');
    const day = String(now.getDate()).padStart(2, '0');
    const hours = String(now.getHours()).padStart(2, '0');
    const minutes = String(now.getMinutes()).padStart(2, '0');
    const seconds = String(now.getSeconds()).padStart(2, '0');
    
    return `${year}-${month}-${day}T${hours}:${minutes}:${seconds}`;
  };
  

  // Fetch existing subworks
  const fetchSubworks = async (reviseId, workOrderId) => {
    if (!reviseId || !workOrderId) {
      console.error("Missing reviseId or workOrderId for fetchSubworks", { reviseId, workOrderId });
      return;
    }

    setIsLoading(true);
    try {
      console.log(`Fetching subworks with reviseId: ${reviseId}, workOrderId: ${workOrderId}`);
      const response = await fetch(`${API_BASE_URL}/subwork/${reviseId}/${workOrderId}`, {
        headers: {
          "Authorization": `Bearer ${jwtToken}`,
          "Accept": "*/*"
        }
      });

      if (!response.ok) {
        throw new Error(`HTTP error! Status: ${response.status}`);
      }

      const data = await response.json();
      if (Array.isArray(data)) {
        console.log("Subworks loaded:", data);
        setSubworks(data);
        
        // After getting subworks, fetch items for each subwork
        for (const subwork of data) {
          fetchItemsForSubwork(subwork.id);
        }
        
      
      } else {
        console.error("Unexpected response format. Expected an array but got:", data);
        toast.error('Failed to load subworks data');
      }
    } catch (error) {
      console.error("Error fetching subworks:", error);
      toast.error(`Error loading subworks: ${error.message}`);
    } finally {
      setIsLoading(false);
    }
  };
    const fetchItemsForSubwork = async (subworkId) => {
  try {
    // Use the same endpoint that works in loadSubworkItems function
    const response = await fetch(`${API_BASE_URL}/txn-items`, {
      headers: {
        "Authorization": `Bearer ${jwtToken}`,
        "Accept": "*/*"
      }
    });

    if (!response.ok) {
      throw new Error(`HTTP error! Status: ${response.status}`);
    }

    const allItems = await response.json();
    
    // Filter items that belong to this subwork (same logic as loadSubworkItems)
    const subworkItems = allItems.filter(item => item.fkSubworkId === subworkId);
    
    if (Array.isArray(subworkItems)) {
      // Update the items state with the filtered items
      setItems(prevItems => {
        const updatedItems = { ...prevItems };
        updatedItems[subworkId] = subworkItems;
        
        // Store items in localStorage for the measurement table to access
        updateSubRecordCache(subworkId, subworkItems);
        
        return updatedItems;
      });
    }
  } catch (error) {
    console.error(`Error fetching items for subwork ${subworkId}:`, error);
  }
};

  const updateSubRecordCache = (subworkId, itemsData) => {
    try {
      const cacheKey = `subwork_items_${subworkId}`;
      localStorage.setItem(cacheKey, JSON.stringify(itemsData));
      console.log(`Cached items for subwork ${subworkId}:`, itemsData);
    } catch (error) {
      console.error('Error caching subwork items:', error);
    }
  };
 const getSubRecordCache = (subworkId) => {
    try {
      const cacheKey = `subwork_items_${subworkId}`;
      const cachedData = localStorage.getItem(cacheKey);
      if (cachedData) {
        return JSON.parse(cachedData);
      }
    } catch (error) {
      console.error('Error reading subwork items cache:', error);
    }
    return [];
  };
   const clearSubRecordCache = (subworkId) => {
    try {
      const cacheKey = `subwork_items_${subworkId}`;
      localStorage.removeItem(cacheKey);
    } catch (error) {
      console.error('Error clearing subwork items cache:', error);
    }
  };

  const handleSearchChange = (subworkId, query) => {
    setSearchQueries((prev) => ({
      ...prev,
      [subworkId]: query.toLowerCase(),
    }));
  };
  // Fetch item options for dropdown
  const fetchItemOptions = async () => {
    try {
      // Changed API endpoint to fetch from detailed items master endpoint
      const response = await fetch(`${API_BASE_URL}/master/detailedItems`, {
        headers: {
          "Authorization": `Bearer ${jwtToken}`,
          "Accept": "*/*"
        }
      });

      if (!response.ok) {
        throw new Error(`HTTP error! Status: ${response.status}`);
      }

      const data = await response.json();
      if (Array.isArray(data)) {
        // Store the complete items data
        setItemOptions(data);
        console.log("Detailed items loaded successfully:", data.length);
      } else {
        console.error("Unexpected response format for item options. Expected an array but got:", data);
        toast.error('Failed to load item options');
      }
    } catch (error) {
      console.error("Error fetching item options:", error);
      toast.error(`Error loading item options: ${error.message}`);
    }
  };


// const saveItemProperties = async (itemId, consumptionMaterials) => {
//   try {
//     // Prevent saving if we don't have valid data
//     if (!itemId || !Array.isArray(consumptionMaterials) || consumptionMaterials.length === 0) {
//       console.warn("Cannot save item properties: invalid data provided");
//       return [];
//     }
    
//     console.log(`Saving ${consumptionMaterials.length} properties for item ${itemId}`);
    
//     // Prepare properties to save
//     const propertiesToSave = consumptionMaterials.map(material => ({
//       id: 0,  // New record
//       txnItemId: itemId,
//       material: material.materialName || "N/A",
//       materialConstant: material.constant || 0,
//       materialUnit: material.materialUnit || "N/A"
//     }));
    
//     console.log("Saving item properties:", propertiesToSave);
    
//     // Save each property
//     const savePromises = propertiesToSave.map(async (property) => {
//       const response = await fetch(`${API_BASE_URL}/txn-item-properties`, {
//         method: 'POST',
//         headers: {
//           "Authorization": `Bearer ${jwtToken}`,
//           "Content-Type": "application/json",
//           "Accept": "*/*"
//         },
//         body: JSON.stringify(property)
//       });
      
//       if (!response.ok) {
//         const errorText = await response.text();
//         console.error("API error response:", errorText);
//         throw new Error(`Failed to save property: ${response.status}`);
//       }
      
//       return await response.json();
//     });
    
//     const results = await Promise.all(savePromises);
//     console.log("Property save results:", results);
//     toast.success(`Saved ${results.length} material properties`);
//     return results;
    
//   } catch (error) {
//     console.error("Error saving item properties:", error);
//     toast.error(`Failed to save material properties: ${error.message}`);
//     throw error;
//   }
// };








const saveItemProperties = async (itemId, consumptionMaterials) => {
  try {
    // Prevent saving if we don't have valid data
    if (!itemId || !Array.isArray(consumptionMaterials) || consumptionMaterials.length === 0) {
      console.warn("Cannot save item properties: invalid data provided");
      return [];
    }

    console.log(`Saving ${consumptionMaterials.length} properties for item ${itemId}`);

    // First try using the fromConsumption API with ID instead of itemNo
    try {
      
      const detailedItemId = consumptionMaterials[0].id || consumptionMaterials[0].detailedItemId || consumptionMaterials[0].ssrItemId;
      
      const fromConsumptionResponse = await fetch(
        `${API_BASE_URL}/txn-item-properties/fromConsumption?detailsItemId=${detailedItemId}&txtItemId=${itemId}`,
        {
          method: 'POST',
          headers: {
            "Authorization": `Bearer ${jwtToken}`,
            "Accept": "*/*",
            "Content-Type": "application/json"
          }
        }
      );

      if (fromConsumptionResponse.ok) {
        const response = await fromConsumptionResponse.json();
        console.log("Properties saved via fromConsumption API:", response);
        toast.success(`Saved ${consumptionMaterials.length} material properties`);
        return response;
      } else {
        console.warn(`fromConsumption API failed with status: ${fromConsumptionResponse.status}`);
        throw new Error(`API responded with status: ${fromConsumptionResponse.status}`);
      }
    } catch (apiError) {
      console.warn("fromConsumption API failed, falling back to manual save:", apiError);
    }
     
     // Prepare properties to save manually if the API failed
    const propertiesToSave = consumptionMaterials.map(material => ({
      id: 0,  // New record
      txnItemId: itemId,
      material: material.materialName,
      materialConstant: material.constant,
      materialUnit: material.materialUnit
    }));

    console.log("Saving item properties manually:", propertiesToSave);
    
    // Save each property
    const savePromises = propertiesToSave.map(async (property) => {
      const response = await fetch(`${API_BASE_URL}/txn-item-properties`, {
        method: 'POST',
        headers: {
          "Authorization": `Bearer ${jwtToken}`,
          "Content-Type": "application/json", 
          "Accept": "*/*"
        },
        body: JSON.stringify(property)
      });
      
      if (!response.ok) {
        const errorText = await response.text();
        console.error("API error response:", errorText);
        throw new Error(`Failed to save property: ${response.status}`);
      }
      
      return await response.json();
    });
    
    const results = await Promise.all(savePromises);
    console.log("Property save results:", results);
    toast.success(`Saved ${results.length} material properties`);
    return results;
    
  } catch (error) {
    console.error("Error saving item properties:", error);
    toast.error(`Failed to save material properties: ${error.message}`);
    throw error;
  }
};

  // Handle subwork name change
  const handleSubworkNameChange = (e) => {
    const value = e.target.value;
    setSubworkName(value);
    if (value.trim()) {
      setSubworkNameError(false);
    }
  };
//  // Improved viewItemDetails function to correctly fetch and show all item details
// const viewItemDetails = async (item) => {
//   try {
//     // Show loading state immediately
//     setIsLoading(true);
//     setItemDetails({...item});
//     setShowItemDetailsModal(true);
    
//     // Collect all details in one object
//     let combinedDetails = {...item};
    
//     // 1. First fetch the base item data
//     try {
//       const itemResponse = await fetch(`${API_BASE_URL}/txn-items/${item.id}`, {
//         headers: {
//           "Authorization": `Bearer ${jwtToken}`,
//           "Accept": "*/*"
//         }
//       });
      
//       if (itemResponse.ok) {
//         const itemData = await itemResponse.json();
//         console.log("Transaction item data:", itemData);
//         combinedDetails = {...combinedDetails, ...itemData};
//       }
//     } catch (itemError) {
//       console.warn("Error fetching transaction item:", itemError);
//     }
    
//     // 2. Get the detailed item info from master data
//     try {
//       // Find the matching item from our pre-loaded options
//       const detailedItem = itemOptions.find(opt => opt.ssrItemId === item.itemNo);
      
//       if (detailedItem) {
//         console.log("Found detailed master item:", detailedItem);
//         // Add detailed info to our combined object
//         combinedDetails = {
//           ...combinedDetails,
//           description: detailedItem.description || "N/A",
//           pageNo: detailedItem.pageNo || "N/A",
//           additionalSpecification: detailedItem.additionalSpecification || "N/A",
//           smallUnit: detailedItem.smallUnit || "N/A",
//           fullUnit: detailedItem.fullUnit || "N/A",
//           completedRate: detailedItem.completedRate || 0,
//           labourRate: detailedItem.labourRate || 0,
//           scadaFlag: detailedItem.scadaFlag || false,
//           category: detailedItem.category || "N/A",
//           chapterName: detailedItem.chapterName || "N/A",
//           referenceNo: detailedItem.referenceNo || "N/A"
//         };
//       }
//     } catch (detailError) {
//       console.warn("Error finding detailed item:", detailError);
//     }
    
//     // 3. Fetch consumption materials using the provided API
//     try {
//       const detailedItemId = item.itemNo;
      
//       if (detailedItemId) {
//         const materialsResponse = await fetch(
//           `${API_BASE_URL}/master/consumptionMaterialAndRoad/getDetailedItemId?detailedItemId=${detailedItemId}`,
//           {
//             headers: {
//               "Authorization": `Bearer ${jwtToken}`,
//               "Accept": "*/*"
//             }
//           }
//         );
        
//         if (materialsResponse.ok) {
//           const materialsData = await materialsResponse.json();
//           console.log("Consumption materials data:", materialsData);
          
//           // Add materials to the combined details
//           if (Array.isArray(materialsData)) {
//             combinedDetails.consumptionMaterials = materialsData;
//           } else {
//             combinedDetails.consumptionMaterials = [];
//           }
//         }
//       }
//     } catch (materialsError) {
//       console.warn("Error fetching consumption materials:", materialsError);
//       combinedDetails.consumptionMaterials = [];
//     }
    
//     // 4. Check for existing transaction item properties
//     let existingProperties = [];
//     try {
//       const propertiesResponse = await fetch(`${API_BASE_URL}/txn-item-properties/byTxnItem/${item.id}`, {
//         headers: {
//           "Authorization": `Bearer ${jwtToken}`,
//           "Accept": "*/*"
//         }
//       });
      
//       if (propertiesResponse.ok) {
//         existingProperties = await propertiesResponse.json();
//         console.log("Existing transaction item properties:", existingProperties);
        
//         if (Array.isArray(existingProperties) && existingProperties.length > 0) {
//           combinedDetails.itemProperties = existingProperties;
//         }
//       }
//     } catch (propError) {
//       console.warn("Error fetching transaction item properties:", propError);
//     }
    
//     // 5. If no existing properties found, try to fetch and save them using the new API
//     if (!existingProperties || existingProperties.length === 0) {
//       try {
//         const detailedItemId = item.itemNo;
//         const txnItemId = item.id;
        
//         // Using the fromConsumption API endpoint
//         const fromConsumptionResponse = await fetch(
//           `${API_BASE_URL}/txn-item-properties/fromConsumption?detailsItemId=${detailedItemId}&txtItemId=${txnItemId}`,
//           {
//             method: 'POST',
//             headers: {
//               "Authorization": `Bearer ${jwtToken}`,
//               "Accept": "*/*",
//               "Content-Type": "application/json"
//             },
//             body: '' // Empty body as per your API specification
//           }
//         );
        
//         if (fromConsumptionResponse.ok) {
//           // Refetch the properties after creating them from consumption
//           const refreshPropertiesResponse = await fetch(`${API_BASE_URL}/txn-item-properties/byTxnItem/${item.id}`, {
//             headers: {
//               "Authorization": `Bearer ${jwtToken}`,
//               "Accept": "*/*"
//             }
//           });
          
//           if (refreshPropertiesResponse.ok) {
//             const refreshedProperties = await refreshPropertiesResponse.json();
//             console.log("Properties created from consumption:", refreshedProperties);
            
//             if (Array.isArray(refreshedProperties)) {
//               combinedDetails.itemProperties = refreshedProperties;
//             }
//           }
//         }
//       } catch (consumptionError) {
//         console.warn("Error creating properties from consumption:", consumptionError);
        
//         // If the fromConsumption API fails, fall back to the original saveItemProperties method
//         if (combinedDetails.consumptionMaterials && combinedDetails.consumptionMaterials.length > 0) {
//           try {
//             const savedProperties = await saveItemProperties(item.id, combinedDetails.consumptionMaterials);
//             if (Array.isArray(savedProperties)) {
//               combinedDetails.itemProperties = savedProperties;
//             }
//           } catch (saveError) {
//             console.warn("Error falling back to saveItemProperties:", saveError);
//           }
//         }
//       }
//     }
    
//     // Finally update the state with all collected data
//     // Ensure all fields have default values for missing data
//     const finalDetails = {
//       ...combinedDetails,
//       description: combinedDetails.description || combinedDetails.descriptionOfItem || "N/A",
//       pageNo: combinedDetails.pageNo || "N/A",
//       additionalSpecification: combinedDetails.additionalSpecification || "N/A",
//       smallUnit: combinedDetails.smallUnit || "N/A",
//       fullUnit: combinedDetails.fullUnit || "N/A",
//       completedRate: combinedDetails.completedRate || 0,
//       labourRate: combinedDetails.labourRate || 0,
//       category: combinedDetails.category || "N/A",
//       chapterName: combinedDetails.chapterName || "N/A",
//       referenceNo: combinedDetails.referenceNo || "N/A",
//       consumptionMaterials: combinedDetails.consumptionMaterials || [],
//       itemProperties: combinedDetails.itemProperties || []
//     };
    
//     setItemDetails(finalDetails);
    
//   } catch (error) {
//     console.error("Error processing item details:", error);
//     toast.error(`Failed to load complete item details: ${error.message}`);
//   } finally {
//     setIsLoading(false);
//   }
// };

// Updated viewItemDetails function with correct API usage
/**
 * Enhanced View Item Details Function
 * Fetches comprehensive item information from multiple APIs
 */
const viewItemDetails = async (item) => {
  try {
    // Show loading state immediately
    setIsLoading(true);
    setItemDetails({ ...item });
    setShowItemDetailsModal(true);
    
    // Initialize combined details with base item data
    let combinedDetails = { ...item };
    let detailedItemId = null;
    
    // Parallel API calls for better performance
    const apiCalls = [];
    
    // 1. Fetch base transaction item data
    apiCalls.push(
      fetch(`${API_BASE_URL}/txn-items/${item.id}`, {
        headers: {
          "Authorization": `Bearer ${jwtToken}`,
          "Accept": "application/json"
        }
      }).then(response => ({ type: 'item', response }))
    );
    
    // 2. Find detailed item from master data (synchronous operation)
    try {
      const detailedItem = itemOptions.find(opt => opt.ssrItemId === item.itemNo);
      
      if (detailedItem) {
        console.log("Found detailed master item:", detailedItem);
        detailedItemId = detailedItem.detailedItemId;
        
        // Merge detailed item information
        combinedDetails = {
          ...combinedDetails,
          description: detailedItem.description || combinedDetails.descriptionOfItem || "N/A",
          pageNo: detailedItem.pageNo || "N/A",
          additionalSpecification: detailedItem.additionalSpecification || "N/A",
          smallUnit: detailedItem.smallUnit || "N/A",
          fullUnit: detailedItem.fullUnit || "N/A",
          completedRate: detailedItem.completedRate || 0,
          labourRate: detailedItem.labourRate || 0,
          scadaFlag: detailedItem.scadaFlag || false,
          category: detailedItem.category || "N/A",
          chapterName: detailedItem.chapterName || "N/A",
          referenceNo: detailedItem.referenceNo || "N/A",
          detailedItemId: detailedItemId
        };
        
        // Add consumption materials API call if detailedItemId exists
        if (detailedItemId) {
          apiCalls.push(
            fetch(`${API_BASE_URL}/master/consumptionMaterialAndRoad/getDetailedItemId/ ${detailedItemId}`, {
              headers: {
                "Authorization": `Bearer ${jwtToken}`,
                "Accept": "application/json"
              }
            }).then(response => ({ type: 'materials', response }))
          );
        }
      } else {
        console.warn(`No detailed item found for itemNo: ${item.itemNo}`);
      }
    } catch (detailError) {
      console.warn("Error finding detailed item:", detailError);
    }
    
    // 3. Add existing transaction item properties API call
    apiCalls.push(
      fetch(`${API_BASE_URL}/txn-item-properties/byTxnItem/${item.id}`, {
        headers: {
          "Authorization": `Bearer ${jwtToken}`,
          "Accept": "application/json"
        }
      }).then(response => ({ type: 'properties', response }))
    );
    
    // Execute all API calls in parallel
    const results = await Promise.allSettled(apiCalls);
    
    // Process API results
    let existingProperties = [];
    let consumptionMaterials = [];
    
    for (const result of results) {
      if (result.status === 'fulfilled') {
        const { type, response } = result.value;
        
        try {
          if (response.ok) {
            const data = await response.json();
            
            switch (type) {
              case 'item':
                console.log("Transaction item data:", data);
                combinedDetails = { ...combinedDetails, ...data };
                break;
                
              case 'materials':
                console.log("Consumption materials data:", data);
                consumptionMaterials = Array.isArray(data) ? data : [];
                break;
                
              case 'properties':
                console.log("Existing transaction item properties:", data);
                existingProperties = Array.isArray(data) ? data : [];
                break;
            }
          } else {
            console.warn(`API call failed for ${type}:`, response.status, response.statusText);
          }
        } catch (parseError) {
          console.warn(`Error parsing ${type} response:`, parseError);
        }
      } else {
        console.warn("API call rejected:", result.reason);
      }
    }
    
    // Add consumption materials to combined details
    combinedDetails.consumptionMaterials = consumptionMaterials;
    
    // 4. Handle transaction item properties
    if (existingProperties.length > 0) {
      combinedDetails.itemProperties = existingProperties;
    } else if (consumptionMaterials.length > 0) {
      // Create properties from consumption materials if none exist
      try {
        console.log("Creating transaction item properties from consumption materials...");
        
        const propertyCreationPromises = consumptionMaterials.map(async (material) => {
          const propertyData = {
            id: 0,
            txnItemId: item.id,
            material: material.materialName || "Unknown Material",
            materialConstant: material.constant || 0,
            materialUnit: material.materialUnit || "Unit"
          };
          
          try {
            const createResponse = await fetch(`${API_BASE_URL}/txn-item-properties`, {
              method: 'POST',
              headers: {
                "Authorization": `Bearer ${jwtToken}`,
                "Accept": "application/json",
                "Content-Type": "application/json"
              },
              body: JSON.stringify(propertyData)
            });
            
            if (createResponse.ok) {
              const createdProperty = await createResponse.json();
              console.log("Created property:", createdProperty);
              return createdProperty;
            } else {
              console.warn(`Failed to create property for material ${material.materialName}:`, 
                         createResponse.status, createResponse.statusText);
              return null;
            }
          } catch (createError) {
            console.warn(`Error creating property for material ${material.materialName}:`, createError);
            return null;
          }
        });
        
        const createdProperties = await Promise.allSettled(propertyCreationPromises);
        const successfulProperties = createdProperties
          .filter(result => result.status === 'fulfilled' && result.value !== null)
          .map(result => result.value);
        
        if (successfulProperties.length > 0) {
          combinedDetails.itemProperties = successfulProperties;
          console.log(`Successfully created ${successfulProperties.length} item properties`);
        } else {
          combinedDetails.itemProperties = [];
        }
        
      } catch (createPropertiesError) {
        console.warn("Error creating properties from consumption materials:", createPropertiesError);
        combinedDetails.itemProperties = [];
      }
    } else {
      combinedDetails.itemProperties = [];
    }
    
    // 5. Finalize details with proper fallbacks
    const finalDetails = {
      ...combinedDetails,
      // Ensure all required fields have proper fallbacks
      description: combinedDetails.description || combinedDetails.descriptionOfItem || "N/A",
      pageNo: combinedDetails.pageNo || "N/A",
      additionalSpecification: combinedDetails.additionalSpecification || "N/A",
      smallUnit: combinedDetails.smallUnit || "N/A",
      fullUnit: combinedDetails.fullUnit || "N/A",
      completedRate: Number(combinedDetails.completedRate) || 0,
      labourRate: Number(combinedDetails.labourRate) || 0,
      scadaFlag: Boolean(combinedDetails.scadaFlag),
      category: combinedDetails.category || "N/A",
      chapterName: combinedDetails.chapterName || "N/A",
      referenceNo: combinedDetails.referenceNo || "N/A",
      consumptionMaterials: combinedDetails.consumptionMaterials || [],
      itemProperties: combinedDetails.itemProperties || [],
      detailedItemId: detailedItemId
    };
    
    // Update state with final details
    setItemDetails(finalDetails);
    
    console.log("Final item details:", finalDetails);
    
    // Show success message if needed
    if (finalDetails.consumptionMaterials.length > 0 || finalDetails.itemProperties.length > 0) {
      toast.success("Item details loaded successfully");
    }
    
  } catch (error) {
    console.error("Error processing item details:", error);
    toast.error(`Failed to load item details: ${error.message}`);
    
    // Set fallback item details in case of complete failure
    setItemDetails({
      ...item,
      description: item.descriptionOfItem || "N/A",
      pageNo: "N/A",
      additionalSpecification: "N/A",
      smallUnit: "N/A",
      fullUnit: "N/A",
      completedRate: 0,
      labourRate: 0,
      scadaFlag: false,
      category: "N/A",
      chapterName: "N/A",
      referenceNo: "N/A",
      consumptionMaterials: [],
      itemProperties: []
    });
  } finally {
    setIsLoading(false);
  }
};

/**
 * Enhanced function to show measurement table for item
 * @param {string|number} itemId - The ID of the item to show measurements for
 */
const showMeasurementTable = (itemId) => {
  if (!itemId) {
    console.warn("Invalid itemId provided to showMeasurementTable");
    return;
  }
  
  setVisibleMeasurements((prev) => ({
    ...prev,
    [itemId]: true
  }));
  
  console.log(`Showing measurement table for item ID: ${itemId}`);
};

/**
 * Helper function to hide measurement table for item
 * @param {string|number} itemId - The ID of the item to hide measurements for
 */
const hideMeasurementTable = (itemId) => {
  if (!itemId) {
    console.warn("Invalid itemId provided to hideMeasurementTable");
    return;
  }
  
  setVisibleMeasurements((prev) => ({
    ...prev,
    [itemId]: false
  }));
  
  console.log(`Hiding measurement table for item ID: ${itemId}`);
};

/**
 * Helper function to toggle measurement table visibility
 * @param {string|number} itemId - The ID of the item to toggle measurements for
 */
const toggleMeasurementTable = (itemId) => {
  if (!itemId) {
    console.warn("Invalid itemId provided to toggleMeasurementTable");
    return;
  }
  
  setVisibleMeasurements((prev) => ({
    ...prev,
    [itemId]: !prev[itemId]
  }));
  
  console.log(`Toggling measurement table for item ID: ${itemId}`);
};


    const formatCurrency = (amount) => {
    return new Intl.NumberFormat('en-IN', {
        style: 'currency',
        currency: 'INR',
        minimumFractionDigits: 2,
        maximumFractionDigits: 2
    }).format(amount);
};
// Add new subwork
  const addSubwork = async () => {
    // Validation
    if (!subworkName.trim()) {
      setSubworkNameError(true);
      toast.error('Please enter a subwork name', {
        icon: '⚠️',
      });
      subworkInputRef.current?.focus();
      return;
    }

    setIsLoading(true);
    const loadingToast = toast.loading('Creating subwork...');
    
    try {
      const userId = localStorage.getItem("id") ; // Default ID if not found
      const currentDate = getFormattedDate();
      
      const subworkPayload = {
        reviseId: parseInt(workOrderInfo.reviseId, 10),
        workorderId: parseInt(workOrderInfo.workOrderId, 10),
        subworkName: subworkName,
        createdDate: currentDate,
        createdBy: userId,
        updatedDate: currentDate,
        updatedBy: userId
      };
      
      const response = await fetch(`${API_BASE_URL}/subwork`, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          "Authorization": `Bearer ${jwtToken}`
        },
        body: JSON.stringify(subworkPayload)
      });
      
      if (!response.ok) {
        const errorText = await response.text();
        console.error("Subwork API Error Response:", errorText);
        throw new Error(`Failed to create subwork: ${response.status} ${response.statusText}`);
      }
      
      const responseData = await response.json();
      // console.log("Subwork Created Successfully:", responseData);
      
      // Update work order status if needed
      if (workOrderInfo.status === "started") {
        updateWorkOrderStatus();
      }
      
      // Update revision status if needed
      if (workOrderInfo.revisionStage === "started") {
        updateRevisionStatus();
      }
      
      // Reset form field
      setSubworkName('');
      
      // Refresh the subworks list
      fetchSubworks(workOrderInfo.reviseId, workOrderInfo.workOrderId);
      
      // Expand the newly added subwork after a short delay
      if (responseData.id) {
        setTimeout(() => {
          setExpandedSubworks(prev => [...prev, responseData.id]);
        }, 500);
      }
      
      toast.dismiss(loadingToast);
      toast.success('Suestimate created successfully!', {
        icon: '✅',
        duration: 3000,
      });
      
    } catch (error) {
      console.error("Error creating subestimate:", error);
      toast.dismiss(loadingToast);
      toast.error(`Failed to add subwork: ${error.message}`, {
        duration: 4000,
      });
    } finally {
      setIsLoading(false);
    }
  };

  // Delete subwork
  const deleteSubwork = async (subworkId) => {
    // Show confirmation toast
    toast((t) => (
      <div className="flex flex-col p-2">
        <p className="mb-2 font-medium">Are you sure you want to delete this subwork?</p>
        <div className="flex justify-end gap-2">
          <button
            onClick={() => toast.dismiss(t.id)}
            className="px-3 py-1 bg-gray-200 rounded hover:bg-gray-300 transition-colors"
          >
            Cancel
          </button>
          <button
            onClick={() => {
              toast.dismiss(t.id);
              confirmDeleteSubwork(subworkId);
            }}
            className="px-3 py-1 bg-red-500 text-white rounded hover:bg-red-600 transition-colors"
          >
            Delete
          </button>
        </div>
      </div>
    ), { duration: 10000 });
  };
  const handleItemSelect = (itemNo) => {
    // Find the selected item from options based on ssrItemId instead of itemNo
    const selectedItem = itemOptions.find(item => item.ssrItemId === itemNo);
    
    // Update the form with the selected item's data using the correct field names
    if (selectedItem) {
      setItemForm(prev => ({
        ...prev,
        itemNo: selectedItem.ssrItemId,
        category: selectedItem.category || '',
        descriptionOfItem: selectedItem.description || '',
        smallUnit: selectedItem.smallUnit || '',
        fullUnit: selectedItem.fullUnit || '',
        completedRate: selectedItem.completedRate || 0,
        labourRate: selectedItem.labourRate || 0,
        additionalSpecification: selectedItem.additionalSpecification || '',
        scadaFlag: selectedItem.scadaFlag || false
      }));
    }
  };
  const confirmDeleteSubwork = async (subworkId) => {
    setIsLoading(true);
    try {
      const resp = await fetch(`${API_BASE_URL}/subwork/${subworkId}`, {
        method: "DELETE",
        headers: { "Authorization": `Bearer ${jwtToken}` }
      });
      if (!resp.ok) throw new Error('Delete failed');
      fetchSubworks(workOrderInfo.reviseId, workOrderInfo.workOrderId);
      toast.success('Deleted!');
    } catch (err) {
      toast.error(err.message);
    } finally {
      setIsLoading(false);
    }
  };

  // Toggle subwork expansion
//   const toggleSubwork = (subworkId) => {
//   const subwork = subworks.find(sw => sw.id === subworkId);
//   setCurrentSubworkForForm(subwork);
  
//   // Your existing toggle logic
//   setExpandedSubworks(prev => 
//     prev.includes(subworkId) 
//       ? prev.filter(id => id !== subworkId)
//       : [...prev, subworkId]
//   );
// };
const toggleSubwork = async (subworkId) => {
    const isCurrentlyExpanded = expandedSubworks.includes(subworkId);
    
    setExpandedSubworks(prev => 
      isCurrentlyExpanded
        ? prev.filter(id => id !== subworkId)
        : [...prev, subworkId]
    );
    
    // If expanding, handle the expansion logic
    if (!isCurrentlyExpanded) {
      await handleSubworkExpand(subworkId);
    }
  };
  // Load items for a subwork
  const loadSubworkItems = async (subworkId) => {
    setIsItemLoading(true);
    
    try {
      const response = await fetch(`${API_BASE_URL}/txn-items`, {
        headers: {
          "Authorization": `Bearer ${jwtToken}`,
          "Accept": "*/*"
        }
      });
      
      if (!response.ok) {
        throw new Error(`HTTP error! Status: ${response.status}`);
      }
      
      const allItems = await response.json();
      
      // Filter items that belong to this subwork
      const subworkItems = allItems.filter(item => item.fkSubworkId === subworkId);
      
      // Update items state
      setItems(prevItems => ({
        ...prevItems,
        [subworkId]: subworkItems
      }));
      
    } catch (error) {
      console.error(`Error loading items for subwork ${subworkId}:`, error);
      toast.error(`Failed to load items for this subwork`);
    } finally {
      setIsItemLoading(false);
    }
  };

  // Add item to a subwork
  const addItemToSubwork = (subworkId) => {
    setCurrentSubworkId(subworkId);
    setItemForm({
      ...itemForm,
      fkSubworkId: subworkId,
      fkWorkorderId: parseInt(workOrderInfo.workOrderId, 10)
    });
    setEditingItemId(null);
    setItemFormErrors({});
    setShowAddItemModal(true);
  };

  // Handle item selection change
  const handleItemChange = (e) => {
    const selectedItemNo = e.target.value;
    // Find by ssrItemId instead of itemNo
    const selectedItemObj = itemOptions.find(item => item.ssrItemId === selectedItemNo);
    
    setItemFormErrors({
      ...itemFormErrors,
      itemNo: false
    });
    
    if (selectedItemObj) {
      setSelectedItem(selectedItemObj);
      setItemForm({
        ...itemForm,
        itemNo: selectedItemObj.ssrItemId,
        category: selectedItemObj.category || '',
        descriptionOfItem: selectedItemObj.description || '',
        smallUnit: selectedItemObj.smallUnit || '',
        fullUnit: selectedItemObj.fullUnit || '',
        completedRate: selectedItemObj.completedRate || 0,
        labourRate: selectedItemObj.labourRate || 0,
        additionalSpecification: selectedItemObj.additionalSpecification || '',
        scadaFlag: selectedItemObj.scadaFlag || false
      });
    } else {
      setSelectedItem(null);
    }
  };

  // Handle form input changes
  const handleItemFormChange = (e) => {
    const { name, value } = e.target;
    setItemForm({
      ...itemForm,
      [name]: value
    });
    
    if (itemFormErrors[name]) {
      setItemFormErrors({
        ...itemFormErrors,
        [name]: false
      });
    }
  };
// And add a function to update item details if the user makes changes in the modal:
const updateItemDetails = async (updatedDetails) => {
  try {
    setIsLoading(true);
    
    // First, check if we have current data to compare
    const currentItem = items[updatedDetails.fkSubworkId]?.find(item => item.id === updatedDetails.id);
    
    if (!currentItem) {
      throw new Error("Could not find current item data");
    }
    
    // Only update the fields we want to allow changing (scadaFlag only)
    const itemToUpdate = {
      ...currentItem,
      scadaFlag: updatedDetails.scadaFlag
    };
    
    // Update the item
    const response = await fetch(`${API_BASE_URL}/txn-items/${updatedDetails.id}`, {
      method: 'PUT',
      headers: {
        "Authorization": `Bearer ${jwtToken}`,
        "Content-Type": "application/json",
        "Accept": "*/*"
      },
      body: JSON.stringify(itemToUpdate)
    });

    if (!response.ok) {
      throw new Error(`HTTP error! Status: ${response.status}`);
    }

    const savedItem = await response.json();
    
    // Update the items state
    setItems(prev => {
      const updatedItems = {...prev};
      const subworkId = savedItem.fkSubworkId;
      
      if (updatedItems[subworkId]) {
        updatedItems[subworkId] = updatedItems[subworkId].map(item => 
          item.id === savedItem.id ? savedItem : item
        );
      }
      
      return updatedItems;
    });

    // If we have consumption materials and they haven't been saved yet, save them
    if (updatedDetails.consumptionMaterials && 
        (!updatedDetails.txnItemProperties || updatedDetails.txnItemProperties.length === 0)) {
      await saveItemProperties(updatedDetails.id, updatedDetails.consumptionMaterials);
    }

    toast.success('Item details updated successfully');
  } catch (error) {
    console.error("Error updating item details:", error);
    toast.error(`Error updating item details: ${error.message}`);
  } finally {
    setIsLoading(false);
  }
};
  // Validate item form
const validateItemForm = () => {
  const errors = {};
  
  if (!itemForm.itemNo?.trim()) {
    errors.itemNo = 'Item number is required';
  }
  
  if (!itemForm.category?.trim()) {
    errors.category = 'Category is required';
  }
  
  if (!itemForm.descriptionOfItem?.trim()) {
    errors.descriptionOfItem = 'Description is required';
  }

  // Validate numeric fields
  if (itemForm.completedRate && (isNaN(itemForm.completedRate) || itemForm.completedRate < 0)) {
    errors.completedRate = 'Completed rate must be a positive number';
  }

  if (itemForm.labourRate && (isNaN(itemForm.labourRate) || itemForm.labourRate < 0)) {
    errors.labourRate = 'Labour rate must be a positive number';
  }

  setItemFormErrors(errors);
  return Object.keys(errors).length === 0;
};

const handleSubworkExpand = async (subworkId) => {
    console.log(`Expanding subwork ${subworkId}`);
    
    // Set current subwork
    const subwork = subworks.find(sw => sw.id === subworkId);
    if (subwork) {
      setCurrentSubworkForForm(subwork);
      setCurrentSubworkId(subworkId);
    }
    
    // Always fetch items when expanding, with force refresh if needed
    await fetchItemsForSubwork(subworkId, false);
  };
  // Save item
// Updated saveItem function with better error handling and data validation
const saveItem = async (isManualSave = false) => {
  // Prevent multiple saves from running simultaneously
  if (isItemLoading) {
    console.log('Save already in progress, skipping...');
    return false;
  }

  // Validate required fields
  const requiredFields = {
    itemNo: itemForm.itemNo?.trim(),
    category: itemForm.category?.trim(),
    descriptionOfItem: itemForm.descriptionOfItem?.trim()
  };

  const missingFields = Object.entries(requiredFields)
    .filter(([key, value]) => !value)
    .map(([key]) => key);

  if (missingFields.length > 0) {
    if (isManualSave) {
      const errorMsg = `Please fill required fields: ${missingFields.join(', ')}`;
      toast.error(errorMsg);
      setItemFormErrors(prev => ({
        ...prev,
        ...missingFields.reduce((acc, field) => ({ ...acc, [field]: true }), {})
      }));
    }
    return false;
  }

  // Ensure we have a valid subwork ID
  const subworkId = currentSubworkId || currentSubworkForForm?.id;
  if (!subworkId) {
    if (isManualSave) {
      toast.error('No subwork selected. Please select a subwork first.');
    }
    return false;
  }

  // Check for duplicates (only for new items)
  if (!editingItemId) {
    const existingItems = items[subworkId] || [];
    const isDuplicate = existingItems.some(item =>
      item.itemNo === itemForm.itemNo.trim()
    );

    if (isDuplicate) {
      if (isManualSave) {
        toast.error(`Item "${itemForm.itemNo}" already exists in this subwork`);
      }
      return false;
    }
  }

  // Clear any pending auto-save timeout to prevent conflicts
  if (autoSaveTimeout) {
    clearTimeout(autoSaveTimeout);
    setAutoSaveTimeout(null);
  }

  setIsItemLoading(true);
  const loadingToast = isManualSave ? toast.loading(editingItemId ? 'Updating item...' : 'Adding item...') : null;

  try {
    const method = editingItemId ? "PUT" : "POST";
    const url = editingItemId
      ? `${API_BASE_URL}/txn-items/${editingItemId}`
      : `${API_BASE_URL}/txn-items`;

    // Prepare the item data with proper validation and type conversion
    const itemData = {
      // Only include id for PUT requests
      ...(editingItemId && { id: parseInt(editingItemId) }),
      srNo: parseInt(itemForm.srNo) || 0,
      itemNo: itemForm.itemNo.trim(),
      category: itemForm.category.trim(),
      descriptionOfItem: itemForm.descriptionOfItem.trim(),
      floorLiftRise: itemForm.floorLiftRise || "Ground floor",
      fkSubworkId: parseInt(subworkId),
      fkWorkorderId: parseInt(workOrderInfo?.workOrderId || itemForm.fkWorkorderId || 0),
      completedRate: parseFloat(itemForm.completedRate) || 0,
      labourRate: parseFloat(itemForm.labourRate) || 0,
      scadaFlag: Boolean(itemForm.scadaFlag),
      smallUnit: itemForm.smallUnit || "",
      fullUnit: itemForm.fullUnit || "",
      additionalSpecification: itemForm.additionalSpecification || ""
    };

    // Add detailedItemId only if it exists and is valid
    if (itemForm.detailedItemId && parseInt(itemForm.detailedItemId) > 0) {
      itemData.detailedItemId = parseInt(itemForm.detailedItemId);
    }

    console.log('Sending item data:', itemData);

    const response = await fetch(url, {
      method,
      headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${jwtToken}`
      },
      body: JSON.stringify(itemData)
    });

    const responseText = await response.text();
    console.log('Server response:', response.status, responseText);

    if (!response.ok) {
      let errorMessage = `HTTP error! Status: ${response.status}`;
      try {
        const errorData = JSON.parse(responseText);
        errorMessage = errorData.message || errorData.error || errorData.details || errorMessage;
        console.error('Detailed error:', errorData);
      } catch (e) {
        errorMessage = responseText || errorMessage;
      }
      throw new Error(errorMessage);
    }

    // Parse the response if it's JSON
    let responseData;
    try {
      responseData = JSON.parse(responseText);
      console.log('Parsed response:', responseData);
    } catch (e) {
      responseData = responseText;
    }

    // Update local state only once with the server response
    if (responseData && typeof responseData === 'object') {
      const savedItem = responseData.data || responseData;
      
      if (editingItemId) {
        // Update existing item in the state
        setItems(prevItems => ({
          ...prevItems,
          [subworkId]: (prevItems[subworkId] || []).map(item =>
            item.id === parseInt(editingItemId) ? { ...item, ...savedItem } : item
          )
        }));
      } else {
        // Add new item to the state (only if it doesn't already exist)
        setItems(prevItems => {
          const currentItems = prevItems[subworkId] || [];
          const itemExists = currentItems.some(item => 
            item.id === savedItem.id || item.itemNo === savedItem.itemNo
          );
          
          if (itemExists) {
            console.log('Item already exists in state, skipping add');
            return prevItems;
          }
          
          const newItem = {
            ...savedItem,
            id: savedItem.id || Date.now(), // Fallback ID if not provided
          };
          
          return {
            ...prevItems,
            [subworkId]: [...currentItems, newItem]
          };
        });
      }
    }

    // Reset the form after successful save
    resetItemForm();
    
    // Keep the current subwork active so the form remains visible for adding more items
    setCurrentSubworkId(subworkId);
    
    // Clear editing state
    setEditingItemId(null);

    if (loadingToast) {
      toast.dismiss(loadingToast);
    }
    
    if (isManualSave) {
      // toast.success(`Item ${editingItemId ? "updated" : "added"} successfully!`, {
      //   icon: '✅'
      // });
    }

    return true; // Success

  } catch (error) {
    console.error("Error saving item:", error);
    
    if (loadingToast) {
      toast.dismiss(loadingToast);
    }
    
    if (isManualSave) {
      // More specific error messages
      let errorMsg = error.message;
      if (error.message.includes('500')) {
        errorMsg = 'Server error. Please check your data and try again.';
      } else if (error.message.includes('400')) {
        errorMsg = 'Invalid data. Please check all fields and try again.';
      } else if (error.message.includes('401')) {
        errorMsg = 'Authentication error. Please login again.';
      }
      
      toast.error(`Failed to ${editingItemId ? "update" : "add"} item: ${errorMsg}`);
    }
    return false; // Failure
  } finally {
    setIsItemLoading(false);
  }
};


const refreshItemsDisplay = async (subworkId) => {
  try {
    setIsItemLoading(true);
    
    const response = await fetch(`${API_BASE_URL}/txn-items/subwork/${subworkId}`, {
      headers: {
        "Authorization": `Bearer ${jwtToken}`
      }
    });

    if (response.ok) {
      const fetchedItems = await response.json();
      console.log('Refreshed items:', fetchedItems);
      
      setItems(prevItems => ({
        ...prevItems,
        [subworkId]: Array.isArray(fetchedItems) ? fetchedItems : []
      }));
    } else {
      console.error('Failed to refresh items:', response.status);
    }
  } catch (error) {
    console.error('Error refreshing items:', error);
  } finally {
    setIsItemLoading(false);
  }
};
const handleViewItemDetails = async (item) => {
  try {
    setIsLoading(true);
    
    setItemDetails({...item});
    setShowItemDetailsModal(true);
    
    let detailedItem = null;
    
    try {
      detailedItem = itemOptions.find(opt => opt.ssrItemId === item.itemNo);
      
      if (detailedItem) {
        console.log("Found detailed item:", detailedItem);
        setItemDetails(prevDetails => ({
          ...prevDetails,
          description: detailedItem.description,
          pageNo: detailedItem.pageNo,
          additionalSpecification: detailedItem.additionalSpecification,
          smallUnit: detailedItem.smallUnit,
          fullUnit: detailedItem.fullUnit,
          completedRate: detailedItem.completedRate,
          labourRate: detailedItem.labourRate,
          // ... other properties
        }));
      }
    } catch (error) {
      console.error("Error fetching detailed item:", error);
    }
    
  } catch (error) {
    console.error("Error in handleViewItemDetails:", error);
  } finally {
    setIsLoading(false);
  }
};
useEffect(() => {
  if (currentSubworkId && itemForm.itemNo?.trim() && itemForm.category?.trim() && itemForm.descriptionOfItem?.trim()) {
    autoSaveItem(currentSubworkId);
  }
  
  // Cleanup timeout on unmount
  return () => {
    if (autoSaveTimeout) {
      clearTimeout(autoSaveTimeout);
    }
  };
}, [itemForm.itemNo, itemForm.category, itemForm.descriptionOfItem, currentSubworkId]);

  // Edit item
  const editItem = (item) => {
    setCurrentSubworkId(item.fkSubworkId);
    setItemForm({
      ...item
    });
    setEditingItemId(item.id);
    setItemFormErrors({});
    setShowAddItemModal(true);
  };

  // Delete item
  const deleteItem = async (itemId) => {
    // Show confirmation toast
    toast((t) => (
      <div className="flex flex-col p-2">
        <p className="mb-2 font-medium">Are you sure you want to delete this item?</p>
        <div className="flex justify-end gap-2">
          <button
            onClick={() => toast.dismiss(t.id)}
            className="px-3 py-1 bg-gray-200 rounded hover:bg-gray-300 transition-colors"
          >
            Cancel
          </button>
          <button
            onClick={() => {
              toast.dismiss(t.id);
              confirmDeleteItem(itemId);
            }}
            className="px-3 py-1 bg-red-500 text-white rounded hover:bg-red-600 transition-colors"
          >
            Delete
          </button>
        </div>
      </div>
    ), { duration: 10000 });
  };
const autoSaveItem = async (subworkId) => {
  // Only auto-save if we have required fields
  if (!itemForm.itemNo?.trim() || !itemForm.category?.trim() || !itemForm.descriptionOfItem?.trim()) {
    return;
  }

  // Don't auto-save if we're currently editing an item manually or if save is in progress
  if (editingItemId || isItemLoading) {
    return;
  }

  // Check if item already exists
  const existingItems = items[subworkId] || [];
  const existingItem = existingItems.find(item => item.itemNo === itemForm.itemNo.trim());

  if (existingItem) {
    console.log('Item already exists, skipping auto-save');
    return;
  }

  console.log('Auto-saving item...');
  await saveItem(false); // Pass false to indicate this is not a manual save
};


// Auto-save when form changes
// useEffect(() => {
//   if (currentSubworkId && itemForm.itemNo && itemForm.category && itemForm.descriptionOfItem) {
//     autoSaveItem(currentSubworkId);
//   }
// }, [itemForm, currentSubworkId]);
useEffect(() => {
  return () => {
    if (autoSaveTimeout) {
      clearTimeout(autoSaveTimeout);
    }
  };
}, [autoSaveTimeout]);

  const confirmDeleteItem = async (itemId) => {
    const loadingToast = toast.loading('Deleting item...');
    
    try {
      const response = await fetch(`${API_BASE_URL}/txn-items/${itemId}`, {
        method: "DELETE",
        headers: {
          "Authorization": `Bearer ${jwtToken}`
        }
      });
      
      if (!response.ok) {
        throw new Error(`HTTP error! Status: ${response.status}`);
      }
      
      // Reload items for the current subwork
      const subworkId = Object.keys(items).find(key => 
        items[key].some(item => item.id === itemId)
      );
      
      if (subworkId) {
        loadSubworkItems(parseInt(subworkId, 10));
      }
      
      toast.dismiss(loadingToast);
      toast.success('Item deleted successfully!');
    } catch (error) {
      console.error("Error deleting item:", error);
      toast.dismiss(loadingToast);
      toast.error(`Failed to delete item: ${error.message}`);
    }
  };

  // Update work order status
  const updateWorkOrderStatus = () => {
    console.log("Updating work order status...");
    // This would be an API call to update the work order status
  };
// You can also implement this as a separate reusable component

// Update the ItemDropdown component to use the correct field for display
const SearchableDropdown = ({ 
  options, 
  value, 
  onChange, 
  searchKey, 
  displayKey, 
  placeholder = "Select an option...",
  error,
  className = "",
  disabled = false
}) => {
  const [isOpen, setIsOpen] = useState(false);
  const [searchQuery, setSearchQuery] = useState('');
  const dropdownRef = useRef(null);

  // Debug logging
  useEffect(() => {
    console.log('SearchableDropdown props:', {
      optionsLength: options?.length,
      searchKey,
      displayKey,
      value,
      sampleOption: options?.[0]
    });
  }, [options, searchKey, displayKey, value]);

  // Close dropdown when clicking outside
 useEffect(() => {
    const handleClickOutside = (event) => {
      if (dropdownRef.current && !dropdownRef.current.contains(event.target)) {
        setIsOpen(false);
      }
    };

    document.addEventListener("mousedown", handleClickOutside);
    return () => {
      document.removeEventListener("mousedown", handleClickOutside);
    };
  }, []);

  // Ensure options is an array
  const safeOptions = Array.isArray(options) ? options : [];

  // Remove duplicates based on displayKey
  const uniqueOptions = safeOptions.reduce((acc, current) => {
    if (!current || typeof current !== 'object') return acc;
    
    const key = current[displayKey];
    if (key && !acc.find(item => item[displayKey] === key)) {
      acc.push(current);
    }
    return acc;
  }, []);

  // Filter options based on search query
 const filteredOptions = uniqueOptions.filter(option => {
    if (!option || typeof option !== 'object') return false;
    
    // Check if the search field exists and has a value
    const searchValue = option[searchKey];
    if (!searchValue) return false;

    // Convert to string and perform case-insensitive search
    const searchString = String(searchValue).toLowerCase();
    const queryString = searchQuery.toLowerCase().trim();
    
    return searchString.includes(queryString);
  }).sort((a, b) => {
    const aVal = String(a[displayKey] || '');
    const bVal = String(b[displayKey] || '');
    return aVal.localeCompare(bVal, undefined, { numeric: true });
  });

  // Debug filtered options
   useEffect(() => {
    if (isOpen) {
      console.log('Filtered options:', {
        totalOptions: safeOptions.length,
        uniqueOptions: uniqueOptions.length,
        filteredOptions: filteredOptions.length,
        searchQuery,
        sampleFiltered: filteredOptions.slice(0, 3)
      });
    }
  }, [filteredOptions, isOpen, searchQuery]);

   const handleOptionClick = (option) => {
    console.log('Option selected:', option);
    onChange(option[displayKey], option);
    setIsOpen(false);
    setSearchQuery('');
  };

   return (
    <div className={`relative ${className}`} ref={dropdownRef}>
      <div 
        className={`w-full p-2 border ${error ? 'border-red-500' : 'border-gray-300'} rounded flex justify-between items-center cursor-pointer bg-white hover:border-gray-400 transition-colors ${disabled ? 'bg-gray-100 cursor-not-allowed' : ''}`}
        onClick={() => !disabled && setIsOpen(!isOpen)}
      >
        <span className={value ? 'text-gray-800' : 'text-gray-500'}>
          {value || placeholder}
        </span>
        <FontAwesomeIcon 
          icon={isOpen ? faChevronUp : faChevronDown} 
          className="text-gray-400 text-sm"
        />
      </div>
      
      
      {isOpen && !disabled && (
        <div className="absolute z-50 w-full mt-1 bg-white border border-gray-300 rounded shadow-lg" 
             style={{ maxHeight: '300px', overflowY: 'auto' }}>
          <div className="sticky top-0 bg-white p-2 border-b border-gray-200">
            <input
              type="text"
              placeholder={`Search ${displayKey}...`}
              value={searchQuery}
              onChange={(e) => setSearchQuery(e.target.value)}
              className="w-full p-2 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-gray-400"
              onClick={(e) => e.stopPropagation()}
              autoFocus
            />
          </div>
          <div>
            {filteredOptions.length > 0 ? (
              filteredOptions.map((option, index) => (
                <div
                  key={`${option[displayKey]}-${index}`}
                  className="p-2 hover:bg-gray-100 cursor-pointer border-b border-gray-50 last:border-b-0"
                  onClick={() => handleOptionClick(option)}
                >
                  <div className="font-medium text-gray-800">
                    {option[displayKey]}
                  </div>
                  {displayKey !== 'descriptionOfItem' && option.description && (
                    <div className="text-xs text-gray-500 mt-1 truncate">
                      {option.description}
                    </div>
                  )}
                  {/* Show additional info for debugging */}
                  {process.env.NODE_ENV === 'development' && (
                    <div className="text-xs text-blue-500 mt-1">
                      ID: {option.ssrItemId} | Cat: {option.category}
                    </div>
                  )}
                </div>
              ))
            ) : (
              <div className="p-3 text-gray-500 italic text-center">
                {safeOptions.length === 0 
                  ? "No options available"
                  : `No ${displayKey} found matching "${searchQuery}"`
                }
              </div>
            )}
          </div>
        </div>
      )}
      
      {error && (
        <p className="text-red-500 text-xs mt-1">{error}</p>
      )}
    </div>
  );
};
const handleItemFieldChange = (field, value, subworkId, selectedOption = null) => {
  console.log('handleItemFieldChange called:', { field, value, subworkId, selectedOption });
  
  // Set the current subwork ID when field changes
  if (subworkId) {
    setCurrentSubworkId(subworkId);
  }

  // Find the selected item from options if not provided
  let selectedItem = selectedOption;
  
  if (!selectedItem && value) {
    // Try to find the item by the specific field that was changed
    selectedItem = itemOptions.find(option => {
      if (field === 'itemNo') return option.ssrItemId === value;
      if (field === 'category') return option.category === value;
      if (field === 'descriptionOfItem') return option.description === value; // Fixed: use 'description' from API
      return false;
    });
    
    console.log('Found selected item:', selectedItem);
  }

  if (selectedItem) {
    // Auto-populate all fields when an item is selected
    const updatedForm = {
      ...itemForm,
      itemNo: selectedItem.ssrItemId || '',
      category: selectedItem.category || '',
      descriptionOfItem: selectedItem.description || '', // Fixed: map 'description' to 'descriptionOfItem'
      labourRate: selectedItem.labourRate || 0,
      completedRate: selectedItem.completedRate || 0,
      smallUnit: selectedItem.smallUnit || '',
      fullUnit: selectedItem.fullUnit || '',
      additionalSpecification: selectedItem.additionalSpecification || '',
      scadaFlag: selectedItem.scadaFlag || false,
      fkSubworkId: subworkId || currentSubworkId || currentSubworkForForm?.id,
      fkWorkorderId: itemForm.fkWorkorderId || 0,
      detailedItemId: selectedItem.detailedItemId || 0
    };

    setItemForm(updatedForm);
    setSelectedItem(selectedItem);
    
    // Clear any field errors
    setItemFormErrors(prev => ({ 
      ...prev, 
      itemNo: false,
      category: false,
      descriptionOfItem: false
    }));
    
    console.log('Item form updated with complete item:', updatedForm);
  } else {
    // If no matching item found, just update the specific field
    setItemForm(prev => ({ 
      ...prev, 
      [field]: value,
      fkSubworkId: subworkId || currentSubworkId || currentSubworkForForm?.id
    }));
    
    console.log('Updated single field:', field, value);
  }
};
const resetItemForm = () => {
  setItemForm({
    id: 0,
    srNo: 0,
    itemNo: "",
    category: "",
    descriptionOfItem: "",
    floorLiftRise: "Ground floor",
    fkSubworkId: 0,
    fkWorkorderId: parseInt(workOrderInfo?.workOrderId || 0),
    completedRate: 0,
    labourRate: 0,
    scadaFlag: false,
    smallUnit: "",
    fullUnit: "",
    additionalSpecification: "",
    detailedItemId: 0
  });
  setSelectedItem(null);
  setItemFormErrors({});
  // Don't reset currentSubworkId here - let it be managed by saveItem function
  
  // Clear auto-save timeout
  if (autoSaveTimeout) {
    clearTimeout(autoSaveTimeout);
    setAutoSaveTimeout(null);
  }
};
  // Update revision status
  const updateRevisionStatus = () => {
    console.log("Updating revision status...");
    // This would be an API call to update the revision status
  };
  const navigateToAbstract = async () => {
    const loadingToast = toast.loading('Preparing abstract...');
    
    try {
      // Get all subworks with their items
      const allSubworkIds = Object.keys(items);
      
      if (allSubworkIds.length === 0) {
        toast.error("No subworks or items found to generate abstract");
        toast.dismiss(loadingToast);
        return;
      }
      
      // Collect all items with their measurements
      const allItemsWithMeasurements = await Promise.all(
        allSubworkIds.flatMap(subworkId =>
          (items[subworkId] || []).map(async (item) => {
            try {
              const response = await fetch(`${API_BASE_URL}/txn-items-mts/ByItemId/${item.id}`, {
                headers: {
                  Authorization: `Bearer ${jwtToken}`,
                  "Content-Type": "application/json",
                },
              });
              
              if (!response.ok) {
                console.warn(`Could not fetch measurements for item ${item.id}`);
                return { ...item, measurements: [] };
              }
              
              const measurements = await response.json();
              const quantity = Array.isArray(measurements) 
                ? measurements.reduce((sum, m) => sum + parseFloat(m.quantity || 0), 0) 
                : 0;
                
              return { 
                ...item, 
                measurements: Array.isArray(measurements) ? measurements : [],
                quantity: quantity,
                amount: quantity * item.labourRate
              };
            } catch (err) {
              console.error("Error fetching measurements for item", item.id, err);
              return { ...item, measurements: [] };
            }
          })
        )
      );  // Store data in localStorage for the abstract page to use
      localStorage.setItem("abstractItems", JSON.stringify(allItemsWithMeasurements));
      localStorage.setItem("abstractWorkName", workOrderInfo.nameOfWork);
      localStorage.setItem("abstractWorkOrderId", workOrderInfo.autogenerated);
      localStorage.setItem("abstractArea", workOrderInfo.area || "900");
     
      toast.dismiss(loadingToast);
      toast.success("Abstract data prepared successfully!");
      
      // Navigate to the abstract page
      navigate("/abstract");
    } catch (error) {
      console.error("Error preparing abstract data:", error);
      toast.dismiss(loadingToast);
      toast.error(`Failed to prepare abstract: ${error.message}`);
    }
  };
  const debugItemOptions = () => {
  console.log('=== Item Options Debug ===');
  console.log('Total options:', itemOptions?.length);
  console.log('Sample options:', itemOptions?.slice(0, 3));
  console.log('Unique descriptions:', [...new Set(itemOptions?.map(item => item.description))].slice(0, 5)); // Fixed: use 'description'
  console.log('Unique categories:', [...new Set(itemOptions?.map(item => item.category))].slice(0, 5));
  console.log('Unique item numbers:', [...new Set(itemOptions?.map(item => item.ssrItemId))].slice(0, 5));
};
useEffect(() => {
  if (itemOptions?.length > 0) {
    debugItemOptions();
  }
}, [itemOptions]);
  const navigateToCoverPage = async () => {
    const loadingToast = toast.loading('Preparing cover page data...');
    
    try {
      // Calculate the grand total from all items for the estimate cost
      const allSubworkIds = Object.keys(items);
      let grandTotal = 0;
      
      // Calculate grand total from all items
      for (const subworkId of allSubworkIds) {
        const subworkItems = items[subworkId] || [];
        for (const item of subworkItems) {
          // Get item quantity from measurements
          let quantity = 0;
          try {
            const response = await fetch(`${API_BASE_URL}/txn-items-mts/ByItemId/${item.id}`, {
              headers: {
                Authorization: `Bearer ${jwtToken}`,
                "Content-Type": "application/json",
              },
            });
            
            if (response.ok) {
              const measurements = await response.json();
              quantity = Array.isArray(measurements) 
                ? measurements.reduce((sum, m) => sum + parseFloat(m.quantity || 0), 0) 
                : 0;
            }
          } catch (err) {
            console.error("Error fetching measurements for item", item.id, err);
          }
          
          // Calculate item amount and add to grand total
          const amount = quantity * (parseFloat(item.labourRate) || 0);
          grandTotal += amount;
        }
      }
      
      // Store required data in localStorage for the cover page
      const constructionEstimate = {
        grandTotal,
        workOrderId: workOrderInfo.autogenerated,
        items: allSubworkIds.length
      };
      
      localStorage.setItem("constructionEstimate", JSON.stringify(constructionEstimate));
      localStorage.setItem("abstractWorkName", workOrderInfo.nameOfWork);
      localStorage.setItem("abstractWorkOrderId", workOrderInfo.autogenerated);
      localStorage.setItem("abstractArea", workOrderInfo.area || "900");
      localStorage.setItem("abstractSSR", workOrderInfo.ssr);
      // area: data.area || prevInfo.area,
      toast.dismiss(loadingToast);
      toast.success("Cover page data prepared successfully!");
      
      // Navigate to the cover page
      navigate("/cover");
    } catch (error) {
      console.error("Error preparing cover page data:", error);
      toast.dismiss(loadingToast);
      toast.error(`Failed to prepare cover page data: ${error.message}`);
    }
  };
    const toggleMeasurements = (itemId) => {
    setVisibleMeasurements(prev => ({
      ...prev,
      [itemId]: !prev[itemId]
    }));
  };
  // In your subestimate component, when passing props to measurement component

const calculateItemRA = async (item) => {
    try {
        const response = await fetch(`${API_BASE_URL}/txn-items-mts/ByItemId/${item.id}`, {
            headers: {
                Authorization: `Bearer ${jwtToken}`,
                "Content-Type": "application/json",
            },
        });
        
        if (!response.ok) {
            return 0;
        }
        
        const measurements = await response.json();
        const quantity = Array.isArray(measurements) 
            ? measurements.reduce((sum, m) => sum + parseFloat(m.quantity || 0), 0) 
            : 0;
            
        return quantity * item.labourRate;
    } catch (err) {
        console.error("Error calculating RA for item", item.id, err);
        return 0;
    }
};
const checkItemOptionsHealth = () => {
  if (!itemOptions || itemOptions.length === 0) {
    console.warn('itemOptions is empty or undefined');
    return false;
  }
  
  const hasDescriptions = itemOptions.some(item => item.description); // Fixed: use 'description'
  if (!hasDescriptions) {
    console.warn('No items have description field');
    return false;
  }
  
  return true;
};
useEffect(() => {
  checkItemOptionsHealth();
}, [itemOptions]);
const renderMeasurementTable = (item) => {
  if (!visibleMeasurements[item.id]) return null;
  
  const currentSubworkItems = items[item.fkSubworkId] || [];
  const previousItems = [];
  
  for (const subItem of currentSubworkItems) {
    if (subItem.id === item.id) break;
    previousItems.push(subItem);
  }
  
  return (
    <MeasurementTable 
      itemId={item.id} 
      token={jwtToken} 
      unitLabel={item.smallUnit || "Cu.M."} 
      multifloor={true}
      itemsFromParent={previousItems} 
      completedRate={item.completedRate || 0}
      onQuantityUpdate={(totalQuantity) => {
        setItemTotalQuantities(prev => ({
          ...prev,
          [item.id]: totalQuantity
        }));
      }}
    />
  );
};
useEffect(() => {
  if (itemOptions?.length > 0) {
    debugItemOptions();
  }
}, [itemOptions]);
const handleAutoSave = () => {
  // Clear any existing timeout
  if (autoSaveTimeout) {
    clearTimeout(autoSaveTimeout);
    setAutoSaveTimeout(null);
  }

  // Don't auto-save if currently loading or editing
  if (isItemLoading || editingItemId) {
    return;
  }

  // Check if all required fields are filled
  const hasRequiredFields = itemForm.itemNo?.trim() &&
                            itemForm.category?.trim() &&
                            itemForm.descriptionOfItem?.trim();

  if (hasRequiredFields && currentSubworkId) {
    // Set a timeout to auto-save after 3 seconds of inactivity (increased from 2)
    const timeoutId = setTimeout(() => {
      autoSaveItem(currentSubworkId);
      setAutoSaveTimeout(null);
    }, 3000);
    
    setAutoSaveTimeout(timeoutId);
  }
};
const calculateSubworkTotal = (subworkId) => {
  const subworkItems = items[subworkId] || [];
  return subworkItems.reduce((total, item) => {
    const quantity = itemTotalQuantities[item.id] || 0;
    const rate = item.completedRate || 0;
    return total + (quantity * rate);
  }, 0);
};
  const navigateToPdfPreview = async () => {
    const allSubworkIds = Object.keys(items);
    
    if (allSubworkIds.length === 0) {
      toast.error("No subworks or items found to generate PDF");
      return;
    }
  
    const allItemsWithMeasurements = await Promise.all(
      allSubworkIds.flatMap(subworkId =>
        (items[subworkId] || []).map(async (item) => {
          try {
            const response = await fetch(`${API_BASE_URL}/txn-items-mts/ByItemId/${item.id}`, {
              headers: {
                Authorization: `Bearer ${jwtToken}`,
                "Content-Type": "application/json",
              },
            });
            const measurements = await response.json();
            return { ...item, measurements: Array.isArray(measurements) ? measurements : [] };
          } catch (err) {
            console.error("Error fetching measurements for item", item.id, err);
            return { ...item, measurements: [] };
          }
        })
      )
    );
  
    localStorage.setItem("subRecordCache", JSON.stringify(allItemsWithMeasurements));
    localStorage.setItem("pdfWorkName", workOrderInfo.nameOfWork);
    localStorage.setItem("pdfWorkOrderId", workOrderInfo.autogenerated);
    localStorage.setItem("pdfRevisionNumber", workOrderInfo.reviseno);
  
    toast.success("Preparing full PDF...");
    navigate("/pdf-preview");
  };


  // Debug logging to check current state
  console.log("Current workOrderInfo.reviseno:", workOrderInfo.reviseno);
  console.log("Full workOrderInfo state:", workOrderInfo);
  
  return (
    <motion.div 
      className="container mx-auto p-4"
      initial="hidden"
      animate="visible"
      variants={containerVariants}
    >
      {/* Toast Container */}
      <Toaster 
        position="top-right"
        toastOptions={{
          duration: 3000,
          style: {
            background: '#fff',
            color: '#333',
            boxShadow: '0 4px 6px rgba(0, 0, 0, 0.1)',
            borderRadius: '8px',
            padding: '12px 16px',
          },
          success: {
            style: {
              border: '1px solid #10B981',
            },
          },
          error: {
            style: {
              border: '1px solid #EF4444',
            },
          },
        }}
      />
      
      {/* Stepper Component */}
      <motion.div 
        className="mb-6 mt-2 p-4 border border-gray-300 rounded bg-white shadow-md"
        variants={itemVariants}
      >
        <Stepper currentStep={2} />
      </motion.div>
      
      {/* Work Order Information Card */}
<motion.div className="bg-white border border-gray-200 rounded-lg p-6 mb-6 shadow-md" variants={itemVariants}>
      <h2 className="text-xl font-bold mb-4 text-blue-700 border-b pb-2">Work Order Details</h2>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="flex flex-col">
          <span className="text-gray-600 text-sm">Estimate ID</span>
          <span className="font-semibold text-lg">{workOrderInfo.autogenerated || 'N/A'}</span>
        </div>
        <div className="flex flex-col">
          <span className="text-gray-600 text-sm">Revision No</span>
          <span className="font-semibold text-lg" key={workOrderInfo.reviseno}>
            {workOrderInfo.reviseno || 'Loading...'}
          </span>
        </div>
        <div className="flex flex-col">
          <span className="text-gray-600 text-sm">Specified Area</span>
          <span className="font-semibold text-lg">{workOrderInfo.area || 'N/A'}</span>
        </div>
        <div className="col-span-2 flex flex-col">
          <span className="text-gray-600 text-sm">Name Of Work</span>
          <span className="font-semibold text-lg">{workOrderInfo.nameOfWork || 'N/A'}</span>
        </div>
        <div className="flex flex-col">
          <span className="text-gray-600 text-sm">SSR</span>
          <span className="font-semibold text-lg">{workOrderInfo.ssr || 'N/A'}</span>
        </div>
      </div>
      <Toaster />
    </motion.div>

      {/* Add Subwork Form */}
      <motion.div 
        className="mb-6 p-6 border border-gray-300 rounded-lg bg-white shadow-md"
        variants={itemVariants}
      >
        <h2 className="text-lg font-bold mb-4 text-blue-700">Add New SubEstimate</h2>
        <div className="flex flex-wrap items-center gap-4">
          <div className="w-full md:w-1/2 lg:w-2/3">
            <div className="relative">
              <input
                type="text"
                value={subworkName}
                onChange={handleSubworkNameChange}
                className={`w-full p-3 border ${subworkNameError ? 'border-red-500' : 'border-gray-300'} rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all`}
                placeholder="Enter Subwork Name"
                ref={subworkInputRef}
              />
              {subworkNameError && (
                <p className="text-red-500 text-sm mt-1 absolute">SubEstimate name is required</p>
              )}
            </div>
          </div>
          <div className="w-full md:w-1/3 lg:w-1/4">
            <button
              className={`w-full ${subworkName.trim() ? 'bg-blue-600 hover:bg-blue-700' : 'bg-blue-400 cursor-not-allowed'} text-white py-3 px-4 rounded-lg transition-colors duration-300 flex items-center justify-center font-medium shadow-md`}
              onClick={addSubwork}
              disabled={isLoading || !subworkName.trim()}
            >
              {isLoading ? (
                <FontAwesomeIcon icon={faSpinner} spin className="mr-2" />
              ) : (
                <FontAwesomeIcon icon={faPlus} className="mr-2" />
              )}
              Create SubEstimate
            </button>
          </div>
        </div>
      </motion.div>

{/* Subworks Section */}
<motion.div className="mb-6 bg-white border border-gray-200 rounded-lg shadow-md overflow-hidden" variants={itemVariants}>
  <div className="bg-blue-700 text-white py-3 px-6 flex justify-between items-center">
    <h2 className="text-lg font-bold">SubEstimate</h2>
    <span className="bg-blue-600 text-white px-3 py-1 rounded-full text-sm font-medium">
      {subworks.length} {subworks.length === 1 ? 'SubEstimate' : 'SubEstimates'}
    </span>
  </div>
{isLoading ? (
  <div className="p-8 text-center">
    <FontAwesomeIcon icon={faSpinner} spin className="text-gray-600 text-3xl mb-2" />
    <p className="text-gray-600">Loading SubEstimate...</p>
  </div>
) : subworks.length === 0 ? (
  <div className="p-8 text-center">
    <FontAwesomeIcon icon={faInfoCircle} className="text-gray-400 text-3xl mb-2" />
    <p className="text-gray-500">No subestimate added yet. Create one using the form above.</p>
  </div>
) : (
  <div className="space-y-4">
    {subworks.map(subwork => (
      <div key={subwork.id} className="bg-white rounded shadow overflow-hidden">
        <div 
          className="p-4 bg-gray-50 flex justify-between items-center cursor-pointer"
          onClick={() => toggleSubwork(subwork.id)}
        >
          <div className="flex items-center flex-1">
            <div className="mr-2">
              {expandedSubworks.includes(subwork.id) ? 
                <FontAwesomeIcon icon={faChevronDown} /> : 
                <FontAwesomeIcon icon={faChevronRight} />
              }
            </div>
            <div className="flex-1">
              <h3 className="font-medium text-lg">{subwork.subworkName}</h3>
              {/* Subwork Total Amount Display */}
              <div className="mt-2 p-2 bg-green-50 border border-green-200 rounded-md">
                <div className="flex justify-between items-center">
                  <span className="text-sm font-medium text-gray-700">SubEstimate Total Amount:</span>
                  <span className="text-lg font-bold text-green-600">
                    ₹{calculateSubworkTotal(subwork.id).toFixed(2)}
                  </span>
                </div>
              </div>
            </div>
          </div>
          <button
            onClick={(e) => {
              e.stopPropagation();
              deleteSubwork(subwork.id);
            }}
            className="ml-4 text-red-600 hover:text-red-800 transition-colors"
          >
            <FontAwesomeIcon icon={faTrash} />
          </button>
        </div>

        <AnimatePresence>
          {expandedSubworks.includes(subwork.id) && (
            <motion.div
              initial={{ height: 0, opacity: 0 }}
              animate={{ height: 'auto', opacity: 1 }}
              exit={{ height: 0, opacity: 0 }}
              transition={{ duration: 0.3 }}
              className="overflow-hidden bg-gray-50"
            >
              <div className="p-6 border-t border-gray-200">
                {isItemLoading ? (
                  <div className="text-center py-6">
                    <FontAwesomeIcon icon={faSpinner} spin className="text-gray-600 text-xl mb-2" />
                    <p className="text-gray-600">Loading items...</p>
                  </div>
                ) : (
                  <>
                    <div className="flex justify-between items-center mb-4">
                      <h4 className="text-lg font-semibold text-gray-700">
                        Items for: {subwork.subworkName}
                      </h4>
                    </div> 

                    {/* Table Section */}
                    <table className="min-w-full bg-white border border-gray-200 rounded-lg overflow-hidden">
                      <thead className="bg-gray-100">
                        <tr>
                          <th className="py-3 px-4 text-left font-medium text-gray-700">Sr. No</th>
                          <th className="py-3 px-4 text-left font-medium text-gray-700">Item No</th>
                          <th className="py-3 px-4 text-left font-medium text-gray-700">Category</th>
                          <th className="py-3 px-4 text-left font-medium text-gray-700">Description</th>
                          <th className="py-3 px-4 text-center font-medium text-gray-700">Actions</th>
                        </tr>
                      </thead>
                      <tbody>
                        
                        {/* List of existing items */}
                        {(items[subwork.id] || [])
                          .filter(item => {
                            const query = searchQueries[subwork.id] || '';
                            return (
                              item.itemNo?.toLowerCase().includes(query.toLowerCase()) ||
                              item.category?.toLowerCase().includes(query.toLowerCase()) ||
                              item.descriptionOfItem?.toLowerCase().includes(query.toLowerCase())
                            );
                          })
                          .map((item, index) => (
                            <React.Fragment key={item.id}>
                              {/* Item information row */}
                              <tr className={`border-b border-gray-100 hover:bg-gray-50 ${editingItemId === item.id ? 'bg-amber-50' : ''}`}>
                               <td className="py-3 px-4 text-gray-700">
                                  <div className="flex flex-col items-center">
                                    <span>{index + 1}</span>
                                    {/* Measurement expand/collapse button */}
                                    <button
                                      onClick={(e) => {
                                        e.stopPropagation();
                                        setVisibleMeasurements(prev => ({
                                          ...prev,
                                          [item.id]: !prev[item.id]
                                        }));
                                      }}
                                      className="text-black-600 hover:text-black-800 transition-colors mt-1"
                                      title={visibleMeasurements[item.id] ? "Hide Measurements" : "Show Measurements"}
                                    >
                                      <FontAwesomeIcon 
                                        icon={visibleMeasurements[item.id] ? faChevronDown : faChevronRight} 
                                      />
                                    </button>
                                  </div>
                                </td>
                                <td className="py-3 px-4 font-medium text-gray-800">{item.itemNo}</td>
                                <td className="py-3 px-4 text-gray-700">{item.category}</td>
                                <td className="py-3 px-4 text-gray-700">{item.descriptionOfItem}</td>
                                <td className="py-3 px-4 text-center">
                                  <div className="flex justify-center space-x-3">
                                    <button
                                      onClick={(e) => {
                                        e.stopPropagation();
                                        editItem(item);
                                      }}
                                      className="text-amber-600 hover:text-amber-800 transition-colors"
                                      title="Edit"
                                    >
                                      <FontAwesomeIcon icon={faEdit} />
                                    </button>
                                    <button
                                      onClick={(e) => {
                                        e.stopPropagation();
                                        deleteItem(item.id);
                                      }}
                                      className="text-red-600 hover:text-red-800 transition-colors"
                                      title="Delete"
                                    >
                                      <FontAwesomeIcon icon={faTrash} />
                                    </button>
                                    <button
                                      onClick={(e) => {
                                        e.stopPropagation();
                                        viewItemDetails(item);
                                      }}
                                      className="text-gray-600 hover:text-gray-800 transition-colors"
                                      title="View Details"
                                    >
                                      <FontAwesomeIcon icon={faInfoCircle} />
                                    </button>
                                   
                                  </div>
                                </td>
                              </tr>
                              
                              {/* MeasurementTable row - Show if expanded */}
{visibleMeasurements[item.id] && (
  <tr>
    <td colSpan="5">
      <div className="p-4 bg-gray-50 border-t">
        <div className="flex justify-between items-center mb-3">
          <h5 className="text-md font-medium text-gray-700">
            Measurements for: {item.itemNo}
          </h5>
          <button
            onClick={() => setVisibleMeasurements(prev => ({
              ...prev,
              [item.id]: false
            }))}
            className="text-gray-500 hover:text-gray-700"
            title="Hide Measurements"
          >
            <FontAwesomeIcon icon={faTimes} />
          </button>
        </div>
        
        {/* Measurement Table */}
        <MeasurementTable 
          itemId={item.id} 
          token={jwtToken} 
          unitLabel={item.smallUnit || "Cu.M."} 
          multifloor={true}
          itemsFromParent={(() => {
            // Calculate previous items for this specific item
            const currentSubworkItems = items[subwork.id] || [];
            const previousItems = [];
            for (const subItem of currentSubworkItems) {
              if (subItem.id === item.id) break;
              previousItems.push(subItem);
            }
            return previousItems;
          })()}
          completedRate={item.completedRate || 0}
          onQuantityUpdate={(totalQuantity) => {
            // Update the total quantity for this item
            setItemTotalQuantities(prev => ({
              ...prev,
              [item.id]: totalQuantity
            }));
          }}
        />
      </div>
    </td>
  </tr>
)}
                            {visibleMeasurements[item.id] && (
  <tr>
    <td colSpan="5">
      <div className="px-4 pb-4 bg-gray-50">
        {/* Total RA Display */}
        <div className="p-3 bg-blue-50 border border-blue-200 rounded-lg">
         
          <div className="mt-2 grid grid-cols-4 gap-4 text-sm">
            <div>
              <span className="font-medium text-gray-700">Rate:</span>
              <div className="text-blue-600 font-semibold">
                ₹{(item.completedRate || 0).toFixed(2)}
              </div>
            </div>
            <div>
              <span className="font-medium text-gray-700">Quantity:</span>
              <div className="text-blue-600 font-semibold">
                {(itemTotalQuantities[item.id] || 0).toFixed(3)} {item.smallUnit || "Cu.M."}
              </div>
            </div>
            <div>
              <span className="font-medium text-gray-700">Total Amount:</span>
              <div className="text-green-600 font-bold text-lg">
                ₹{((item.completedRate || 0) * (itemTotalQuantities[item.id] || 0)).toFixed(2)}
              </div>
            </div>
            
          </div>
        </div>
      </div>
    </td>
  </tr>
)}
                            </React.Fragment>
                          ))}
                        
                        {/* Always visible Add Item Row */}
                        <tr className="bg-slate-50 border-2 border-slate-200 hover:bg-slate-100 transition-colors">
                          <td className="py-3 px-4 text-center">
                            <FontAwesomeIcon 
                              icon={editingItemId ? faEdit : faPlus} 
                              className={editingItemId ? "text-amber-600" : "text-slate-600"} 
                            />
                          </td>
                        <td className="py-3 px-4">
  <SearchableDropdown
    options={itemOptions}
    value={itemForm.itemNo}
    onChange={(value, option) => {
      handleItemFieldChange('itemNo', value, subwork.id, option);
      handleAutoSave();
    }}
    searchKey="ssrItemId"
    displayKey="ssrItemId"
    placeholder="Select Item No"
    error={itemFormErrors.itemNo}
    className="w-full"
    showAllOnFocus={true} // Add this prop if supported
    minSearchLength={0}   // Add this prop if supported
  />

                          </td>
                         <td className="py-3 px-4">
  <SearchableDropdown
    options={itemOptions}
    value={itemForm.category}
    onChange={(value, option) => {
      handleItemFieldChange('category', value, subwork.id, option);
      handleAutoSave();
    }}
    searchKey="category"
    displayKey="category"
    placeholder="Select Category"
    error={itemFormErrors.category}
    className="w-full"
    showAllOnFocus={true} // Show all options when focused
    minSearchLength={0}   // Show options without requiring search text
    filterUnique={true}   // Only show unique categories
  />
                          </td>
                          <td className="py-3 px-4">
                     <SearchableDropdown
    options={itemOptions}
    value={itemForm.descriptionOfItem}
    onChange={(value, option) => {
      handleItemFieldChange('descriptionOfItem', value, subwork.id, option);
      handleAutoSave();
    }}
    searchKey="description"
    displayKey="description"
    placeholder="Select Description"
    error={itemFormErrors.descriptionOfItem}
    className="w-full"
    showAllOnFocus={true} // Show all options when focused
    minSearchLength={0} 
    filterUnique={true}   // Show options without requiring search text
  />
                          </td>
                          <td className="py-3 px-4 text-center">
                           <div className="flex justify-center items-center space-x-2">
      {/* Auto-save indicator */}
      {autoSaveTimeout && (
        <div className="flex items-center space-x-1 text-blue-600">
          <FontAwesomeIcon icon={faSpinner} spin className="text-sm" />
          <span className="text-xs">Auto-saving...</span>
        </div>
      )}
      
      {/* Show checkmark when all required fields are filled */}
      {itemForm.itemNo?.trim() && itemForm.category?.trim() && itemForm.descriptionOfItem?.trim() && !autoSaveTimeout && (
        <div className="flex items-center space-x-1 text-green-600">
          <FontAwesomeIcon icon={faCheck} className="text-sm" />
          <span className="text-xs">Ready</span>
        </div>
      )}
                              {editingItemId && (
                                <button
                                  onClick={() => {
                                    setEditingItemId(null);
                                    resetItemForm();
                                  }}
                                  className="bg-gray-600 text-white px-3 py-1.5 rounded hover:bg-gray-700 transition-colors"
                                  title="Cancel Edit"
                                >
                                  <FontAwesomeIcon icon={faTimes} />
                                </button>
                              )}
                            </div>
                          </td>
                        </tr>
                      </tbody>
                    </table>
                  </>
                )}
              </div>
            </motion.div>
          )}
        </AnimatePresence>
      </div>
    ))}
  </div>
)}
</motion.div>
       

{/* Item Details Modal with loading state and materials */}
{showItemDetailsModal && (
  <div className="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
    <div className="bg-white rounded-lg shadow-lg w-full max-w-4xl overflow-hidden">
      <div className="flex justify-between items-center bg-blue-600 text-white p-4">
        <h3 className="text-lg font-semibold">Item Details</h3>
        <button 
          onClick={() => setShowItemDetailsModal(false)}
          className="text-white hover:text-gray-200"
        >
          <FontAwesomeIcon icon={faTimes} />
        </button>
      </div>
      
      {isLoading ? (
        <div className="p-6 flex justify-center items-center h-64">
          <div className="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-blue-500"></div>
        </div>
      ) : itemDetails ? (
        <div className="p-6 max-h-[70vh] overflow-y-auto">
          <form>
            {/* Basic Item Info */}
            <div className="grid grid-cols-2 gap-4 mb-4">
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">Item No</label>
                <input
                  type="text"
                  name="itemNo"
                  value={itemDetails.itemNo || "N/A"}
                  disabled
                  className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                />
              </div>
              
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">Category</label>
                <input
                  type="text"
                  name="category"
                  value={itemDetails.category || "N/A"}
                  disabled
                  className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                />
              </div>
            </div>
            
            <div className="mb-4">
              <label className="block text-sm font-medium text-gray-700 mb-1">Description</label>
              <textarea
                name="descriptionOfItem"
                value={itemDetails.descriptionOfItem }
                disabled
                className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                rows="3"
              ></textarea>
            </div>
            
            {/* Editable Fields */}
            <div className="grid grid-cols-2 gap-4 mb-4">
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">Floor/Lift Rise</label>
                <select
                  name="floorLiftRise"
                  value={itemDetails.floorLiftRise || "Ground floor"}
                  onChange={(e) => setItemDetails({...itemDetails, floorLiftRise: e.target.value})}
                  className="w-full p-2 border border-gray-300 rounded"
                >
                  <option value="Ground floor">Ground floor</option>
                  <option value="First floor">First floor</option>
                  <option value="Second floor">Second floor</option>
                  <option value="Third floor">Third floor</option>
                  <option value="Basement">Basement</option>
                </select>
              </div>
              
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">SCADA Flag</label>
                <select
                  name="scadaFlag"
                  value={itemDetails.scadaFlag ? "true" : "false"}
                  onChange={(e) => setItemDetails({...itemDetails, scadaFlag: e.target.value === "true"})}
                  className="w-full p-2 border border-gray-300 rounded"
                >
                  <option value="false">No</option>
                  <option value="true">Yes</option>
                </select>
              </div>
            </div>
            
            {/* Rate Info */}
            <div className="grid grid-cols-2 gap-4 mb-4">
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">Labour Rate</label>
                <input
                  type="number"
                  name="labourRate"
                  value={itemDetails.labourRate || 0}
                  disabled
                  className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                />
              </div>
              
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">Completed Rate</label>
                <input
                  type="number"
                  name="completedRate"
                  value={itemDetails.completedRate || 0}
                  disabled
                  className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                />
              </div>
            </div>
            
            {/* Unit Info */}
            <div className="grid grid-cols-2 gap-4 mb-4">
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">Small Unit</label>
                <input
                  type="text"
                  name="smallUnit"
                  value={itemDetails.smallUnit || "N/A"}
                  disabled
                  className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                />
              </div>
              
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1">Full Unit</label>
                <input
                  type="text"
                  name="fullUnit"
                  value={itemDetails.fullUnit || "N/A"}
                  disabled
                  className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                />
              </div>
            </div>
            
            {/* Additional Fields - Always show them with fallback to N/A */}
            <div className="mb-4">
              <label className="block text-sm font-medium text-gray-700 mb-1">Page No</label>
              <input
                type="text"
                name="pageNo"
                value={itemDetails.pageNo || "N/A"}
                disabled
                className="w-full p-2 border border-gray-300 rounded bg-gray-100"
              />
            </div>
            
            <div className="mb-4">
              <label className="block text-sm font-medium text-gray-700 mb-1">Chapter Name</label>
              <input
                type="text"
                name="chapterName"
                value={itemDetails.chapterName || "N/A"}
                disabled
                className="w-full p-2 border border-gray-300 rounded bg-gray-100"
              />
            </div>
            
            <div className="mb-4">
              <label className="block text-sm font-medium text-gray-700 mb-1">Reference No</label>
              <input
                type="text"
                name="referenceNo"
                value={itemDetails.referenceNo || "N/A"}
                disabled
                className="w-full p-2 border border-gray-300 rounded bg-gray-100"
              />
            </div>
            
            {/* Additional Specification */}
            <div className="mb-4">
              <label className="block text-sm font-medium text-gray-700 mb-1">Additional Specification</label>
              <textarea
                name="additionalSpecification"
                value={itemDetails.additionalSpecification || "N/A"}
                disabled
                className="w-full p-2 border border-gray-300 rounded bg-gray-100"
                rows="2"
              ></textarea>
            </div>
            
            {/* Consumption Materials Section - Always show, even if empty */}
            <div className="mt-6 mb-4">
              <h4 className="text-lg font-semibold mb-2 border-b pb-2">Consumption Materials</h4>
              <div className="bg-gray-50 p-3 rounded border">
                {itemDetails.consumptionMaterials && itemDetails.consumptionMaterials.length > 0 ? (
                  <table className="min-w-full divide-y divide-gray-200">
                    <thead>
                      <tr>
                        <th className="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Material</th>
                        <th className="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Item ID</th>
                        <th className="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Unit</th>
                        <th className="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Constant</th>
                      </tr>
                    </thead>
                    <tbody className="divide-y divide-gray-200">
                      {itemDetails.consumptionMaterials.map((material, index) => (
                        <tr key={`material-${index}`} className={index % 2 === 0 ? 'bg-white' : 'bg-gray-50'}>
                          <td className="px-3 py-2 text-sm text-gray-900">{material.materialName || "N/A"}</td>
                          <td className="px-3 py-2 text-sm text-gray-900">{material.ssrItemId || "N/A"}</td>
                          <td className="px-3 py-2 text-sm text-gray-900">{material.materialUnit || "N/A"}</td>
                          <td className="px-3 py-2 text-sm text-gray-900">{material.constant || "N/A"}</td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                ) : (
                  <div className="text-center py-4 text-gray-500">No consumption materials found</div>
                )}
              </div>
            </div>
            
            {/* Item Properties Section - Always show, even if empty */}
            <div className="mt-6 mb-4">
              <h4 className="text-lg font-semibold mb-2 border-b pb-2">Item Properties</h4>
              <div className="bg-gray-50 p-3 rounded border">
                {itemDetails.itemProperties && itemDetails.itemProperties.length > 0 ? (
                  <table className="min-w-full divide-y divide-gray-200">
                    <thead>
                      <tr>
                        <th className="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Material</th>
                        <th className="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Constant</th>
                        <th className="px-3 py-2 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Unit</th>
                      </tr>
                    </thead>
                    <tbody className="divide-y divide-gray-200">
                      {itemDetails.itemProperties.map((property, index) => (
                        <tr key={`property-${index}`} className={index % 2 === 0 ? 'bg-white' : 'bg-gray-50'}>
                          <td className="px-3 py-2 text-sm text-gray-900">{property.material || "N/A"}</td>
                          <td className="px-3 py-2 text-sm text-gray-900">{property.materialConstant || "N/A"}</td>
                          <td className="px-3 py-2 text-sm text-gray-900">{property.materialUnit || "N/A"}</td>
                        </tr>
                      ))}
                    </tbody>
                  </table>
                ) : (
                  <div className="text-center py-4 text-gray-500">No item properties found</div>
                )}
              </div>
            </div>
          </form>
        </div>
      ) : (
        <div className="p-6 text-center text-red-500">
          <p className="text-lg">Failed to load item details</p>
          <p className="text-sm mt-2">Please try again or contact support</p>
        </div>
      )}
      
      <div className="flex justify-end bg-gray-100 p-4 gap-3">
        <button
          onClick={() => {
            updateItemDetails(itemDetails);
          }}
          className="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 disabled:bg-blue-300"
          disabled={isLoading || !itemDetails}
        >
          {isLoading ? 'Updating...' : 'Update'}
        </button>
        <button
          onClick={() => setShowItemDetailsModal(false)}
          className="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600"
        >
          Close
        </button>
      </div>
    </div>
  </div>
)}

{/* Final PDF Button at Bottom of Page */}
<div className="flex justify-center mt-8 space-x-4">
  <button
    className="bg-green-600 text-white py-3 px-6 rounded-lg hover:bg-green-700 transition-colors flex items-center text-base shadow-md"
    onClick={navigateToPdfPreview}
  >
    <FontAwesomeIcon icon={faFileAlt} className="mr-2" />
    Save and Review
  </button>

  {/* <button 
    onClick={navigateToAbstract}
    className="bg-purple-600 hover:bg-purple-700 text-white py-3 px-6 rounded-lg transition-colors flex items-center text-base shadow-md"
  >
    <FileText className="mr-2" /> Generate Abstract
  </button> */}

  {/* <button
  onClick={navigateToCoverPage}
  className="flex items-center gap-1 px-3 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700 transition-colors"
>
  <FileText size={16} />
  Generate Cover Page
</button> */}
</div>
      </motion.div>
    
  );
};

export default SubEstimateForm;